<wms:FormControl x:Class="Ferretto.Common.Controls.InfoText"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:wms="clr-namespace:Ferretto.Common.Controls"
                 d:DesignHeight="80"
                 d:DesignWidth="300"
                 mc:Ignorable="d">
    <Grid Name="InfoTextGrid">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label x:Name="InnerLabel"
               HorizontalAlignment="{Binding Path=DataContext.HorizontalContentAlignment, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Grid}}}"
               Content="{Binding Path=Label}"
               Style="{DynamicResource ControlLabelStyle}" />
        <Label x:Name="InnerText"
               Grid.Row="1"
               HorizontalAlignment="{Binding Path=DataContext.HorizontalContentAlignment, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Grid}}}" />
    </Grid>
</wms:FormControl>
