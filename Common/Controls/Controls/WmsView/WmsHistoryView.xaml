<UserControl x:Class="Ferretto.Common.Controls.WmsHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wms="clr-namespace:Ferretto.Common.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <wms:ActionBar HorizontalAlignment="Left"
                                   Style="{DynamicResource ActionBarNotCustomizable}">
            <wms:ActionBarItem Command="{Binding BackCommand}"
                                           CustomGlyphSize="24,24"
                                           Glyph="pack://application:,,,/Ferretto.WMS.Themes;component/icons/material/navigation/ic_arrow_back_black_24dp.png"
                                           GlyphSize="Custom" />

        </wms:ActionBar>
        <ContentPresenter Grid.Row="0"
                          Content="{Binding ViewContent}"
                          ContentTemplate="{Binding ViewContentTemplate}"
                          ContentTemplateSelector="{Binding ViewContentTemplateSelector}" />
    </Grid>
</UserControl>
