<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:cs="colorscheme_ignorable_uri"
                    xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
                    xmlns:dxbt="http://schemas.devexpress.com/winfx/2008/xaml/bars/themekeys"
                    xmlns:dxcn="http://schemas.devexpress.com/winfx/2008/xaml/core/internal"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:wms="clr-namespace:Ferretto.Common.Controls;assembly=Ferretto.Common.Controls"
                    mc:Ignorable="cs">

    <DataTemplate x:Key="cardTemplate">
        <StackPanel Orientation="Vertical">
            <Grid Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <wms:WmsImage Grid.Column="0"
                              Width="64"
                              Height="70"
                              Path="{Binding Path=Row.Image}" />
                <StackPanel Grid.Column="1"
                            Margin="12,0,12,0"
                            VerticalAlignment="Top"
                            Orientation="Vertical">
                    <StackPanel Margin="0,0,0,4"
                                Orientation="Horizontal">
                        <TextBlock FontWeight="Bold"
                                   Text="Registration number: " />
                        <TextBlock Text="{Binding Path=Row.RegistrationNumber}"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>

                </StackPanel>
            </Grid>

        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="cardHeaderTemplate">
        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="4,0,0,0"
                       VerticalAlignment="Center"
                       Text="{Binding Path=Row.Nickname}" />
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>
