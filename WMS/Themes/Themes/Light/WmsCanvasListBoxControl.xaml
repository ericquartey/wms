<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrls="clr-namespace:Ferretto.Common.Controls;assembly=Ferretto.Common.Controls"
                    xmlns:dxdst="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">


    <!--<DataTemplate DataType="{x:Type ctrls:WmsCompartmentViewModel}">-->
    <Style x:Key="CanvasItemStyle"
           BasedOn="{StaticResource {dxdst:DefaultStyleThemeKey FullName=System.Windows.Controls.ListBoxItem,
                                                                ThemeName=Office2016White}}"
           TargetType="{x:Type ListBoxItem}">
        <Setter Property="Padding" Value="0" />
        <Setter Property="Canvas.Top" Value="{Binding Top, Mode=TwoWay}" />
        <Setter Property="Canvas.Left" Value="{Binding Left, Mode=TwoWay}" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">

                    <Border x:Name="BorderOfRectangle"
                            Width="{Binding Width, Mode=TwoWay}"
                            Height="{Binding Height, Mode=TwoWay}"
                            BorderBrush="{Binding Selected, Mode=TwoWay}"
                            BorderThickness="{Binding RectangleBorderThickness, Mode=TwoWay}">
                        <Rectangle x:Name="Rectangle"
                                   Fill="{Binding ColorFill, Mode=TwoWay}">
                            <!--<Rectangle.InputBindings>
                            <MouseBinding Command="{Binding IsSelected}"
                                          Gesture="LeftClick" />
                        </Rectangle.InputBindings>-->
                        </Rectangle>
                    </Border>

                    <!--<DataTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Rectangle" Property="Fill" Value="{DynamicResource FillMouseOver}" />
                <Setter TargetName="Rectangle" Property="Stroke" Value="{DynamicResource StrokeMouseOver}" />
                <Setter TargetName="Rectangle" Property="StrokeThickness" Value="5" />
            </Trigger>
            <Trigger Property=""
        </DataTemplate.Triggers>
    </DataTemplate>-->
                    <!--<ControlTemplate x:Key="ListViewNewTemplate"
                     TargetType="{x:Type ctrls:WmsCanvasListBoxControl}">
        <Border BorderBrush="Black"
                BorderThickness="1" />
    </ControlTemplate>-->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderOfRectangle" Property="BorderThickness" Value="3" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="BorderOfRectangle" Property="BorderThickness" Value="8" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
