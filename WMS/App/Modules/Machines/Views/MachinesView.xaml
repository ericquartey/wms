<wms:WmsView x:Class="Ferretto.WMS.Modules.Machines.MachinesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxa="http://schemas.devexpress.com/winfx/2008/xaml/accordion"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:modules="clr-namespace:Ferretto.Common.Utils.Modules;assembly=Ferretto.Common.Utils"
             xmlns:res="clr-namespace:Ferretto.Common.Resources;assembly=Ferretto.Common.Resources"
             xmlns:wms="clr-namespace:Ferretto.Common.Controls;assembly=Ferretto.Common.Controls"
             Title="{x:Static res:Navigation.Machine_Status}"
             MinHeight="300"
             d:DesignHeight="450"
             d:DesignWidth="800"
             EnableHistoryView="True"
             mc:Ignorable="d">


    <Grid>

        <Grid.Resources>
            <wms:BoolToDoubleConverter x:Key="BTOC"
                                       ValueOnFalse="0.3"
                                       ValueOnTrue="1" />

            <DataTemplate x:Key="cardTemplate">

                <Grid DataContext="{Binding Row}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <wms:WmsIcon Grid.Column="0"
                                 Width="90"
                                 Height="90"
                                 Margin="0,35,0,0"
                                 VerticalAlignment="Top"
                                 Opacity="{Binding IsOnLine, Converter={StaticResource BTOC}}"
                                 SymbolName="{Binding Image}" />
                    <StackPanel Grid.Column="1"
                                Margin="12,-10,12,0"
                                VerticalAlignment="Top"
                                Orientation="Vertical">
                        <wms:InfoText Content="{Binding Status}"
                                      IsPillVisible="True" />
                        <wms:InfoText Content="{Binding MaintenanceStatus}"
                                      IsPillVisible="True" />
                        <wms:ProgressBarEdit DisplayFormatString="p0"
                                             EditValue="{Binding Path=FillRate, Mode=OneWay}"
                                             Maximum="100"
                                             Minimum="0" />
                    </StackPanel>
                </Grid>

            </DataTemplate>

            <DataTemplate x:Key="cardHeaderTemplate">
                <Border Background="{DynamicResource CommonSecondaryUltralight}">
                    <Grid Margin="11,3">
                        <TextBlock VerticalAlignment="Center"
                                   FontSize="20"
                                   Opacity="{Binding RowData.Row.IsOnLine, Converter={StaticResource BTOC}}"
                                   Text="{Binding Path=RowData.Row.Nickname}" />
                    </Grid>
                </Border>
            </DataTemplate>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <wms:Expander Grid.Column="0"
                      VerticalAlignment="Stretch"
                      VerticalContentAlignment="Stretch"
                      ExpandDirection="Left"
                      IsExpanded="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <wms:TileBar ItemsSource="{Binding Filters}"
                             SelectedItem="{Binding SelectedFilter, Mode=OneWayToSource}" />

                <dxa:AccordionControl Grid.Row="1"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      IsCollapseButtonVisible="False"
                                      RootItemExpandButtonPosition="Left">
                    <wms:AccordionItem FontSize="12"
                                       FontWeight="Normal"
                                       IsExpandButtonVisible="True"
                                       IsToggleVisible="False"
                                       TitleHeader="{x:Static res:DesktopApp.CustomFilter}">
                        <Border Margin="5"
                                BorderBrush="LightGray"
                                BorderThickness="1">
                            <wms:WmsFilterControl MinWidth="250"
                                                  Filter="{Binding CustomFilter, Mode=OneWayToSource}"
                                                  FilteringContext="{Binding Path=FilteringContext, ElementName=MainGridControl}" />
                        </Border>
                    </wms:AccordionItem>
                </dxa:AccordionControl>
            </Grid>
        </wms:Expander>

        <Grid Grid.Column="1">

            <wms:ActionBar HorizontalAlignment="Left"
                           Style="{DynamicResource ActionBarOverlayedWithGrid}">

                <wms:ActionBarItem Command="{Binding RefreshCommand}"
                                   Content="{x:Static res:General.Refresh}"
                                   CustomGlyphSize="24,24"
                                   Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/navigation/ic_refresh_black_24dp.png"
                                   GlyphSize="Custom" />
            </wms:ActionBar>

            <wms:WmsGridControl x:Name="MainGridControl"
                                MaxWidth="2000"
                                MaxHeight="5000"
                                Margin="3"
                                CurrentItem="{Binding SelectedItem, Mode=TwoWay}"
                                FixedFilter="{Binding OverallFilter, Mode=OneWay}"
                                ItemsSource="{Binding SelectedFilterDataSource, Mode=OneWay}"
                                SelectionMode="Row"
                                ShowBorder="False">

                <dxg:GridControl.View>
                    <dxg:CardView AllowCardResizing="false"
                                  AllowEditing="False"
                                  CardHeaderTemplate="{StaticResource cardHeaderTemplate}"
                                  CardLayout="Rows"
                                  CardMargin="0,0,6,6"
                                  CardTemplate="{StaticResource cardTemplate}"
                                  FixedSize="200"
                                  MinFixedSize="100"
                                  NavigationStyle="Row"
                                  PrintAutoCardWidth="False"
                                  SearchPanelHorizontalAlignment="Right"
                                  SeparatorThickness="0"
                                  ShowCardExpandButton="False"
                                  ShowColumnHeaders="False"
                                  ShowGroupPanel="False"
                                  ShowGroupedColumns="False"
                                  ShowSearchPanelMode="Always"
                                  ShowSelectionRectangle="False">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="RowDoubleClick">
                                <wms:WmsHistoryViewAppear Id="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.CurrentItem.Id, Mode=OneWay}"
                                                          StartModuleName="{wms:NameOf Type={x:Type modules:Machines}}"
                                                          StartViewName="{x:Static modules:Machines.MACHINEDETAILS}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </dxg:CardView>
                </dxg:GridControl.View>

                <wms:WmsGridControl.Columns>
                    <dxg:GridColumn AllowSorting="True"
                                    AllowedBinaryFilters="Equals,DoesNotEqual"
                                    FieldName="MachineTypeDescription" />
                    <dxg:GridColumn AllowSorting="True"
                                    AllowedBinaryFilters="Equals,DoesNotEqual"
                                    FieldName="AreaName" />
                    <dxg:GridColumn AllowSorting="True"
                                    AllowedBinaryFilters="Equals,DoesNotEqual"
                                    FieldName="Nickname" />
                    <dxg:GridColumn AllowSorting="True"
                                    AllowedBinaryFilters="Equals,DoesNotEqual"
                                    FieldName="RegistrationNumber" />
                </wms:WmsGridControl.Columns>

                <wms:WmsGridControl.SortInfo>
                    <dxg:GridSortInfo FieldName="AreaName"
                                      SortOrder="Ascending" />
                    <dxg:GridSortInfo FieldName="Nickname"
                                      SortOrder="Ascending" />
                </wms:WmsGridControl.SortInfo>

            </wms:WmsGridControl>
        </Grid>

    </Grid>
</wms:WmsView>
