<UserControl x:Class="Ferretto.WMS.App.Modules.MasterData.CompartmentTypesToItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wms="clr-namespace:Ferretto.WMS.App.Controls;assembly=Ferretto.WMS.App.Controls"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <wms:WmsGridControl MaxWidth="2000"
                            MaxHeight="2000"
                            Margin="0,0,-2,0"
                            ItemsSource="{Binding UnassociateCompartmentTypesDataSource, Mode=OneWay}"
                            SelectedBusinessObject="{Binding SelectedCompartmentType, Mode=TwoWay}">
            <dxg:GridControl.View>
                <dxg:TableView Name="tableView1" EditorShowMode="MouseDown" />
            </dxg:GridControl.View>
            
            <wms:WmsGridControl.Columns>
                <dxg:GridColumn Width="200*"
                                FieldName="" />

                <dxg:GridColumn Width="120*"
                                FieldName="MaxCapacity">
                    <dxg:GridColumn.DisplayTemplate>
                        <ControlTemplate>
                            <wms:SpinEdit Grid.Row="1"
                                          Grid.Column="1"
                                          Margin="0,0,20,0"
                                          wms:ShowTitle.Show="True"
                                          EditValue="{Binding Model.YPosition, Mode=TwoWay, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                          IsEnabled="False"
                                          Style="{DynamicResource SpinEditDecimalStyle}" />
                        </ControlTemplate>
                    </dxg:GridColumn.DisplayTemplate>
                    &gt;
                </dxg:GridColumn>

                <dxg:GridColumn Width="200*"
                                FieldName="Height" />

                <dxg:GridColumn Width="200*"
                                FieldName="Width" />

                <dxg:GridColumn Width="200*"
                                FieldName="EmptyCompartmentsCount" />

                <dxg:GridColumn Width="200*"
                                FieldName="CompartmentsCount" />

            </wms:WmsGridControl.Columns>

        </wms:WmsGridControl>
    </Grid>
</UserControl>
