<wms:WmsView x:Class="Ferretto.WMS.Modules.MasterData.CompartmentEditStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrls="clr-namespace:Ferretto.Common.Controls.WPF;assembly=Ferretto.Common.Controls.WPF"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:masterdata="clr-namespace:Ferretto.WMS.Modules.MasterData"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:res="clr-namespace:Ferretto.WMS.App.Resources;assembly=Ferretto.WMS.App.Resources"
             xmlns:wms="clr-namespace:Ferretto.WMS.App.Controls;assembly=Ferretto.WMS.App.Controls"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <Grid Height="420">
        <GroupBox Header="{Binding Title}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="10" />
                    <RowDefinition Height="30" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.7*" />
                    <ColumnDefinition Width="0.3*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="1"
                           Grid.ColumnSpan="2"
                           HorizontalAlignment="Left"
                           Text="{x:Static res:MasterData.CreateNewCompartementDesiredCompartmentType}" />

                <GroupBox Grid.Row="2"
                          Header="{x:Static res:MasterData.CompartmentsLayoutOverview}">

                    <ctrls:TrayControl x:Name="TrayControl"
                                       Margin="0,0,0,0"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Stretch"
                                       Compartments="{Binding LoadingUnitDetails.Compartments}"
                                       DimensionHeight="{Binding LoadingUnitDetails.Depth}"
                                       DimensionWidth="{Binding LoadingUnitDetails.Width}"
                                       IsCompartmentSelectable="false"
                                       IsReadOnly="true"
                                       RulerFontSize="{DynamicResource RulerFontSize}"
                                       RulerInfo="mm"
                                       SelectedItem="{Binding SelectedCompartmentTray, Mode=TwoWay}"
                                       ShowBackground="True"
                                       ShowRuler="True" />
                </GroupBox>

                <ScrollViewer Grid.Row="2"
                              Grid.Column="1"
                              Margin="0,-10,0,10"
                              Focusable="False"
                              IsTabStop="True">

                    <masterdata:CompartmentEditView MinHeight="520"
                                                    VerticalAlignment="Stretch"
                                                    VerticalContentAlignment="Stretch"
                                                    DataContext="{Binding CompartmentAdd}" />

                </ScrollViewer>

            </Grid>
        </GroupBox>
    </Grid>

</wms:WmsView>
