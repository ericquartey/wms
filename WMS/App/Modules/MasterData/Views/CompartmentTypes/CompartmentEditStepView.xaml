<wms:WmsView x:Class="Ferretto.WMS.Modules.MasterData.CompartmentEditStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrls="clr-namespace:Ferretto.Common.Controls.WPF;assembly=Ferretto.Common.Controls.WPF"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:res="clr-namespace:Ferretto.Common.Resources;assembly=Ferretto.Common.Resources"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wms="clr-namespace:Ferretto.WMS.App.Controls;assembly=Ferretto.WMS.App.Controls"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <Grid>
        <GroupBox Header="{Binding Title}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="10" />
                    <RowDefinition Height="50" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.7*" />
                    <ColumnDefinition Width="0.3*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="1"
                           Grid.ColumnSpan="2"
                           HorizontalAlignment="Left"
                           Text="{x:Static res:MasterData.ChooseExistingTypesToAssociateCurrentItemAndMaximumCapacity}" />

                <TextBlock Grid.Row="2"
                           HorizontalAlignment="Left"
                           Text="{x:Static res:MasterData.NoUnassociateItemCompartmentTypesAvailable}"
                           Visibility="{Binding Path=HasUnassociatedItemCompartmentTypes, Converter={StaticResource BTVIC}}" />


                <GroupBox Grid.Row="1"
                          Header="{x:Static res:MasterData.CompartmentsLayoutOverview}">
                    <ctrls:TrayControl x:Name="TrayControl"
                                       Margin="0,10,0,0"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Stretch"
                                       CommandDoubleClick="{Binding EditCompartmentCommand}"
                                       Compartments="{Binding Model.Compartments}"
                                       DimensionHeight="{Binding Model.Length}"
                                       DimensionWidth="{Binding Model.Width}"
                                       IsCompartmentSelectable="{Binding IsSidePanelOpen, Converter={StaticResource BNC}}"
                                       RulerFontSize="{DynamicResource RulerFontSize}"
                                       RulerInfo="mm"
                                       SelectedColorFilterFunc="{Binding FilterColorFunc}"
                                       SelectedItem="{Binding SelectedCompartmentTray, Mode=TwoWay}"
                                       ShowBackground="True"
                                       ShowRuler="True">
                        <ctrls:TrayControl.IsReadOnly>
                            <MultiBinding Converter="{StaticResource LOC}">
                                <Binding Path="IsSidePanelOpen" />
                                <Binding Path="IsBusy" />
                            </MultiBinding>
                        </ctrls:TrayControl.IsReadOnly>
                    </ctrls:TrayControl>
                </GroupBox>

            </Grid>
        </GroupBox>
    </Grid>

</wms:WmsView>
