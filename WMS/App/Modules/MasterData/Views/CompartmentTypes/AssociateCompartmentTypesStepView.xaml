<wms:WmsView x:Class="Ferretto.WMS.App.Modules.MasterData.AssociateCompartmentTypesStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:res="clr-namespace:Ferretto.WMS.App.Resources;assembly=Ferretto.WMS.App.Resources"
             xmlns:wms="clr-namespace:Ferretto.WMS.App.Controls;assembly=Ferretto.WMS.App.Controls"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Margin="7,15,0,0"
                   FontSize="{DynamicResource FontSizeMedium}"
                   Text="{Binding Title}" />

        <GroupBox Grid.Row="1"
                  Margin="10,0,0,0"
                  Header="{x:Static res:MasterData.ChooseHowYouWantToAssociateACompartmentTypeToThisItem}">
            <wms:RadioButton Margin="20,10,0,0"
                             DisplayMember="Description"
                             ItemsSource="{Binding Options}"
                             SelectedItem="{Binding SelectedOption, Mode=TwoWay}"
                             ValueMember="Id"
                             FontSize="14"/>
        </GroupBox>
    </Grid>

</wms:WmsView>
