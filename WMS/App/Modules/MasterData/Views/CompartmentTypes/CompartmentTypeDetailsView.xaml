<wms:WmsView x:Class="Ferretto.WMS.Modules.MasterData.CompartmentTypeDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:modules="clr-namespace:Ferretto.Common.Utils.Modules;assembly=Ferretto.Common.Utils"
             xmlns:res="clr-namespace:Ferretto.WMS.App.Resources;assembly=Ferretto.WMS.App.Resources"
             xmlns:wms="clr-namespace:Ferretto.WMS.App.Controls;assembly=Ferretto.WMS.App.Controls"
             Title="{x:Static res:Navigation.CompartmentTypeDetails}"
             MinWidth="200"
             d:DesignHeight="300"
             d:DesignWidth="800"
             EnableHistoryView="True"
             SubTitle="{x:Static res:Navigation.CompartmentTypeDetails}"
             SubTitleVisibility="{Binding Model, Converter={StaticResource RTVC}}"
             mc:Ignorable="d">
    <wms:WmsView.Resources>
        <Style x:Key="inputAreaControlStyle"
               TargetType="{x:Type dxe:FlyoutControl}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="StaysOpen" Value="False" />
            <Setter Property="Settings">
                <Setter.Value>
                    <dxe:FlyoutSettings Placement="Left"
                                        ShowIndicator="True" />
                </Setter.Value>
            </Setter>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid Height="Auto"
                              MinWidth="350"
                              DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.ColumnSpan="2"
                                       FontSize="14"
                                       Text="{x:Static res:BusinessObjects.AssociateNewItem}" />
                            <wms:LookUpEdit Name="ItemLookUp"
                                            Grid.Row="1"
                                            Grid.ColumnSpan="2"
                                            wms:ShowTitle.Show="True"
                                            DisplayMember="Code"
                                            EditValue="{Binding CompartmentTypeInput.ItemId, Mode=TwoWay, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                            ItemsSource="{Binding ItemsDataSource, Mode=OneWay}"
                                            PopupWidth="500"
                                            ValueMember="Id">
                                <wms:LookUpEdit.PopupContentTemplate>
                                    <ControlTemplate>
                                        <wms:WmsGridControl x:Name="PART_GridControl"
                                                            ItemsSource="{Binding ItemsDataSource, Mode=OneWay}">
                                            <wms:WmsGridControl.Columns>
                                                <dxg:GridColumn Width="10*"
                                                                FieldName="Code" />
                                                <dxg:GridColumn Width="20*"
                                                                FieldName="Description" />
                                                <dxg:GridColumn Width="10*"
                                                                FieldName="Image">
                                                    <dxg:GridColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Margin="2"
                                                                        HorizontalAlignment="Center">
                                                                <wms:WmsImage Width="100"
                                                                              Height="100"
                                                                              BorderBrush="{DynamicResource CommonSecondaryLight}"
                                                                              BorderThickness="1"
                                                                              Path="{Binding Data.Image, Mode=OneWay}" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </dxg:GridColumn.CellTemplate>
                                                </dxg:GridColumn>
                                            </wms:WmsGridControl.Columns>
                                            <wms:WmsGridControl.View>
                                                <dxg:TableView RowStyle="{DynamicResource AutoHeightRowControlStyle}" />
                                            </wms:WmsGridControl.View>
                                            <wms:WmsGridControl.SortInfo>
                                                <dxg:GridSortInfo FieldName="Code"
                                                                  SortOrder="Ascending" />
                                            </wms:WmsGridControl.SortInfo>
                                        </wms:WmsGridControl>
                                    </ControlTemplate>
                                </wms:LookUpEdit.PopupContentTemplate>
                            </wms:LookUpEdit>

                            <wms:SpinEdit x:Name="CapacityText"
                                          Grid.Row="2"
                                          HorizontalAlignment="Stretch"
                                          wms:ShowTitle.Show="True"
                                          EditValue="{Binding CompartmentTypeInput.MaxCapacity, Mode=TwoWay, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                          Style="{DynamicResource SpinEditDecimalStyle}" />

                            <TextBlock x:Name="Error"
                                       Grid.Row="3"
                                       Grid.ColumnSpan="2"
                                       Height="50"
                                       Style="{DynamicResource TextBlockErrorMessageStyle}"
                                       Text="{Binding CompartmentTypeInput.Error}"
                                       Visibility="{Binding CompartmentTypeInput.IsValidationEnabled, Converter={StaticResource BTVHC}}" />


                            <wms:ActionBar Grid.Row="4"
                                           Grid.Column="1"
                                           MinWidth="100"
                                           HorizontalAlignment="Right"
                                           Style="{DynamicResource ActionBarNotCustomizable}">
                                <wms:ActionBarItem Command="{Binding AssociateItemCommand}"
                                                   Content="{x:Static res:General.Add}"
                                                   CustomGlyphSize="24,24"
                                                   Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/Action/1x_web/ic_done_black_24dp.png"
                                                   GlyphSize="Custom" />
                                <wms:ActionBarItem Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type dxe:FlyoutControl}}, Path=CloseCommand}"
                                                   Content="{x:Static res:General.Cancel}"
                                                   CustomGlyphSize="24,24"
                                                   Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/content/1x_web/ic_clear_black_24dp.png"
                                                   GlyphSize="Custom" />
                            </wms:ActionBar>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </wms:WmsView.Resources>

    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          IsEnabled="{Binding IsBusy, Converter={StaticResource BNC}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto"
                              MaxWidth="400" />
        </Grid.ColumnDefinitions>

        <dxc:WaitIndicator Grid.Row="1"
                           VerticalAlignment="Center"
                           Panel.ZIndex="1"
                           Content="{x:Static res:DesktopApp.Loading}"
                           DeferedVisibility="{Binding IsBusy}" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <wms:NotifyControl SubTitleDimension="{Binding SubTitleDimension, RelativeSource={RelativeSource AncestorType={x:Type wms:WmsHistoryView}}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

            <wms:ActionBar Grid.Column="1"
                           Grid.ColumnSpan="2"
                           MinWidth="100"
                           Margin="0,10,10,0"
                           HorizontalAlignment="Right"
                           Style="{DynamicResource ActionBarNotCustomizable}">

                <wms:ActionBarItem Command="{Binding DeleteCommand}"
                                   Content="{x:Static res:General.Delete}"
                                   CustomGlyphSize="24,24"
                                   Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/action/1x_web/ic_delete_black_24dp.png"
                                   GlyphSize="Custom"
                                   TooltipText="{Binding DeleteReason}" />

                <wms:ActionBarItem Command="{Binding RefreshCommand}"
                                   Content="{x:Static res:General.Refresh}"
                                   CustomGlyphSize="24,24"
                                   Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/navigation/1x_web/ic_refresh_black_24dp.png"
                                   GlyphSize="Custom" />
            </wms:ActionBar>
        </Grid>
        <ScrollViewer Grid.Row="1"
                      Margin="10,0,10,10"
                      Focusable="False"
                      IsTabStop="True"
                      Visibility="{Binding Model, Converter={StaticResource RTVC}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>



                <Grid Grid.ColumnSpan="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <GroupBox Grid.Row="0"
                              Header="{x:Static res:General.SizeInformation}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <wms:SpinEdit Margin="0,0,20,0"
                                          wms:ShowTitle.Show="True"
                                          EditValue="{Binding Path=Model.Width, Mode=TwoWay, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                          IsEnabled="False" />
                            <wms:SpinEdit Grid.Column="1"
                                          Margin="0,0,20,0"
                                          wms:ShowTitle.Show="True"
                                          EditValue="{Binding Path=Model.Depth, Mode=TwoWay, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                          IsEnabled="False" />

                        </Grid>
                    </GroupBox>
                    <GroupBox Grid.Row="1"
                              Grid.ColumnSpan="2">
                        <GroupBox.HeaderTemplate>
                            <DataTemplate>
                                <Grid Margin="0,-5,0,0"
                                      DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type GroupBox}}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Row="0"
                                               Grid.Column="0"
                                               VerticalAlignment="Center"
                                               Style="{StaticResource GroupBoxHeaderTemplateTextBlockStyle}"
                                               Text="{x:Static res:MasterData.AssociatedItems}" />


                                    <wms:ActionBarInputPanelControl x:Name="flyoutInputPanel"
                                                                    AttachedActionBar="{Binding ElementName=AssociateAreaBar}"
                                                                    AttachedBarItem="AssociateAreaBarItem"
                                                                    FocusedStart="ItemLookUp"
                                                                    IsOpen="{Binding IsAddAssociateItemShown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                    Style="{StaticResource inputAreaControlStyle}" />
                                    <wms:ActionBar x:Name="AssociateAreaBar"
                                                   Grid.Column="1"
                                                   MinWidth="100"
                                                   HorizontalAlignment="Right"
                                                   Style="{DynamicResource ActionBarNotCustomizable}">
                                        <wms:ActionBarItem Command="{Binding OpenCreateNewAssociationCommand}"
                                                           Content="{x:Static res:MasterData.CompartmentTypeAssociateNewItem}"
                                                           CustomGlyphSize="24,24"
                                                           Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/content/1x_web/ic_add_circle_outline_black_24dp.png"
                                                           GlyphSize="Custom"
                                                           Tag="AssociateAreaBarItem" />
                                        <wms:ActionBarItem Command="{Binding DeleteAssociationCommand}"
                                                           Content="{x:Static res:MasterData.CompartmentTypeRemoveAssociation}"
                                                           CustomGlyphSize="24,24"
                                                           Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/action/1x_web/ic_delete_black_24dp.png"
                                                           GlyphSize="Custom" />
                                    </wms:ActionBar>
                                </Grid>
                            </DataTemplate>
                        </GroupBox.HeaderTemplate>
                        <StackPanel Margin="0,10,0,0">
                            <TextBlock Text="{x:Static res:MasterData.CompartmentTypeHasNoItems}"
                                       Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.HasAssociatedItem, Converter={StaticResource BTVIC}}" />

                            <wms:WmsGridControl MinHeight="62"
                                                MaxWidth="2000"
                                                MaxHeight="2000"
                                                ItemsSource="{Binding AssociatedItemsDataSource, Mode=OneWay}"
                                                SelectedBusinessObject="{Binding SelectedAssociatedItem, Mode=TwoWay}"
                                                Visibility="{Binding HasAssociatedItem, Converter={StaticResource BTVC}}">
                                <wms:WmsGridControl.View>
                                    <dxg:TableView>
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="RowDoubleClick">
                                                <wms:WmsHistoryViewAppear Id="{Binding SelectedAssociatedItem.Id, Mode=OneWay}"
                                                                          StartModuleName="{wms:NameOf Type={x:Type modules:MasterData}}"
                                                                          StartViewName="{x:Static modules:MasterData.ITEMDETAILS}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </dxg:TableView>
                                </wms:WmsGridControl.View>

                                <wms:WmsGridControl.Columns>
                                    <dxg:GridColumn Width="100*"
                                                    FieldName="Code" />
                                    <dxg:GridColumn Width="300*"
                                                    FieldName="Description" />
                                    <dxg:GridColumn Width="100*"
                                                    FieldName="ItemCategoryDescription" />
                                    <dxg:GridColumn Width="70*"
                                                    FieldName="AbcClassDescription"
                                                    FilterPopupMode="CheckedList" />
                                    <dxg:GridColumn Width="80*"
                                                    FieldName="MeasureUnitDescription" />
                                    <dxg:GridColumn Width="50*"
                                                    FieldName="TotalStock" />
                                    <dxg:GridColumn Width="100*"
                                                    FieldName="TotalReservedForPick" />
                                    <dxg:GridColumn Width="100*"
                                                    FieldName="TotalReservedToPut" />
                                    <dxg:GridColumn Width="70*"
                                                    FieldName="TotalAvailable" />
                                    <dxg:GridColumn Width="70*"
                                                    FieldName="MaxCapacity" />
                                </wms:WmsGridControl.Columns>

                                <wms:WmsGridControl.SortInfo>
                                    <dxg:GridSortInfo FieldName="Code"
                                                      SortOrder="Ascending" />
                                </wms:WmsGridControl.SortInfo>

                                <wms:WmsGridControl.TotalSummary>
                                    <dxg:GridSummaryItem Alignment="Left"
                                                         DisplayFormat="{x:Static res:DesktopApp.GridRowCounter}"
                                                         SummaryType="Count" />
                                </wms:WmsGridControl.TotalSummary>

                            </wms:WmsGridControl>
                        </StackPanel>
                    </GroupBox>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</wms:WmsView>
