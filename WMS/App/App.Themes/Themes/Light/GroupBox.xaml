<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="GroupBoxHeaderTemplateTextBlockStyle"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="11pt" />
        <Setter Property="FontWeight" Value="DemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource CommonSecondaryDark}" />
    </Style>

    <DataTemplate x:Key="GroupBoxHeaderTemplate">
        <TextBlock Style="{StaticResource GroupBoxHeaderTemplateTextBlockStyle}"
                   Text="{Binding}" />
    </DataTemplate>

    <Style TargetType="GroupBox">
        <Setter Property="HeaderTemplate" Value="{StaticResource GroupBoxHeaderTemplate}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Grid Margin="0,5,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                BorderThickness="0">
                            <ContentPresenter Margin="4"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True" />
                        </Border>

                        <Border Grid.Row="1"
                                BorderThickness="0">
                            <ContentPresenter Margin="4,0,4,4" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
