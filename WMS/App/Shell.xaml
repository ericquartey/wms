<Window x:Class="Ferretto.WMS.App.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:prism="http://www.codeplex.com/prism"   
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        mc:Ignorable="d"
        Title="Shell">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ItemsControl x:Name="MenuContent"  Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" prism:RegionManager.RegionName="Layout.MenuBar"  />
        <ItemsControl x:Name="MainContent"  Grid.Row="1" Grid.Column="1" Grid.RowSpan="3" prism:RegionManager.RegionName="Catalog.Items" />
        <ItemsControl x:Name="StatusBarContent" Grid.Row="2"  prism:RegionManager.RegionName="Layout.StatusBar"  />
    </Grid>
</Window>
