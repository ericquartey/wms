<UserControl x:Class="Ferretto.WMS.App.Controls.NotificationItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:services="clr-namespace:Ferretto.WMS.App.Controls.Services"
             xmlns:wms="clr-namespace:Ferretto.WMS.App.Controls"
             x:Name="NotificationWindow"
             d:DesignHeight="150"
             d:DesignWidth="300"
             Background="Transparent"
             mc:Ignorable="d">

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Button.Click"
                      SourceName="CloseButton">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="NotificationWindow"
                                     Storyboard.TargetProperty="(Grid.Opacity)"
                                     From="1"
                                     To="0"
                                     Duration="0:0:0" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=NotificationWindow, Path=Opacity}"
                             Value="0" />
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <Grid Margin="10,10,10,10"
          Background="Transparent">
        <Border Name="border"
                Margin="10"
                BorderThickness="0"
                CornerRadius="0">
            <Border.Style>

                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=Mode}"
                                     Value="{x:Static services:StatusType.Error}">
                            <Setter Property="Background" Value="{DynamicResource ErrorNotificationStatus}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=Mode}"
                                     Value="{x:Static services:StatusType.Success}">
                            <Setter Property="Background" Value="{DynamicResource SuccessNotificationStatus}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=Mode}"
                                     Value="{x:Static services:StatusType.Warning}">
                            <Setter Property="Background" Value="{DynamicResource WarningNotificationStatus}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=Mode}"
                                     Value="{x:Static services:StatusType.Info}">
                            <Setter Property="Background" Value="{DynamicResource InfoNotificationStatus}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Border.Effect>
                <DropShadowEffect BlurRadius="10"
                                  Opacity="0.8"
                                  ShadowDepth="0" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <wms:WmsIcon x:Name="icon"
                             Grid.RowSpan="2"
                             Width="48"
                             Height="48"
                             Margin="5"
                             ColorizeBrush="White">
                    <wms:WmsIcon.Style>
                        <Style TargetType="wms:WmsIcon">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=Mode}"
                                             Value="{x:Static services:StatusType.Error}">
                                    <Setter Property="SymbolName" Value="Error" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Mode}"
                                             Value="{x:Static services:StatusType.Success}">
                                    <Setter Property="SymbolName" Value="Success" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Mode}"
                                             Value="{x:Static services:StatusType.Warning}">
                                    <Setter Property="SymbolName" Value="Warning" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Mode}"
                                             Value="{x:Static services:StatusType.Info}">
                                    <Setter Property="SymbolName" Value="Info" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </wms:WmsIcon.Style>
                </wms:WmsIcon>

                <TextBlock Grid.Column="1"
                           Margin="3,4,4,2"
                           VerticalAlignment="Center"
                           FontFamily="Arial"
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="White"
                           TextOptions.TextFormattingMode="Display"
                           TextOptions.TextRenderingMode="ClearType"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=Mode}"
                                             Value="{x:Static services:StatusType.Error}">
                                    <Setter Property="Text" Value="Error" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Mode}"
                                             Value="{x:Static services:StatusType.Success}">
                                    <Setter Property="Text" Value="Success" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Mode}"
                                             Value="{x:Static services:StatusType.Warning}">
                                    <Setter Property="Text" Value="Warning" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=Mode}"
                                             Value="{x:Static services:StatusType.Info}">
                                    <Setter Property="Text" Value="Info" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <Button x:Name="CloseButton"
                        Grid.Column="1"
                        Width="16"
                        Height="16"
                        Margin="0,0,12,0"
                        HorizontalAlignment="Right"
                        Click="CloseButton_Click"
                        Style="{StaticResource SystemCloseButton}">
                    <Button.Content>
                        <Grid Width="10"
                              Height="12"
                              RenderTransform="1,0,0,1,0,1">
                            <Path Width="8"
                                  Height="7"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M0,0 L8,7 M8,0 L0,7 Z"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}"
                                  StrokeThickness="1.5" />
                        </Grid>
                    </Button.Content>
                </Button>
                <TextBlock Grid.Row="1"
                           Grid.Column="1"
                           Margin="5,10,5,5"
                           VerticalAlignment="Stretch"
                           FontFamily="Arial"
                           Foreground="White"
                           Text="{Binding Path=Message}"
                           TextOptions.TextFormattingMode="Display"
                           TextOptions.TextRenderingMode="ClearType"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="Wrap" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
