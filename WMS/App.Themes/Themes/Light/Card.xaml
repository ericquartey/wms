<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:cs="colorscheme_ignorable_uri"
                    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
                    xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="cs">

    <ControlTemplate x:Key="{dxgt:GridCardThemeKey ThemeName=Office2016White,
                                                   ResourceKey=ContainerTemplate}"
                     TargetType="{x:Type ContentControl}">
        <Grid x:Name="Root">
            <Border x:Name="Focus"
                    Margin="-2"
                    cs:Name="Border_0008"
                    Background="{DynamicResource CommonSecondaryMedium}"
                    Opacity="0" />
            <Border x:Name="Border"
                    Grid.RowSpan="1"
                    cs:Name="Border_0010"
                    Background="{DynamicResource CommonBackground}"
                    BorderBrush="{DynamicResource CommonSecondaryLight}"
                    BorderThickness="1">
                <ContentPresenter />
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="dxg:GridViewBase.IsFocusedRow" Value="False" />
                </MultiTrigger.Conditions>
            </MultiTrigger>
            <DataTrigger Binding="{Binding Path=(dxg:RowData.RowData).SelectionState, RelativeSource={RelativeSource TemplatedParent}}"
                         Value="Focused">
                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource CommonSecondaryMedium}" />
                <Setter TargetName="Border" Property="Background" Value="{DynamicResource CommonBackground}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=(dxg:RowData.RowData).SelectionState, RelativeSource={RelativeSource TemplatedParent}}"
                         Value="Selected">
                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource CommonSecondaryMedium}" />
                <Setter TargetName="Border" Property="Background" Value="{DynamicResource CommonBackground}" />
            </DataTrigger>
            <Trigger Property="dxg:GridViewBase.IsFocusedRow" Value="True" />
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="{dxgt:GridCardThemeKey ThemeName=Office2016White,
                                                   ResourceKey=HeaderContentTemplate}"
                     TargetType="{x:Type ContentControl}">
        <ContentPresenter x:Name="ContentPresenter1" Margin="0" />
    </ControlTemplate>

    <ControlTemplate x:Key="{dxgt:GridCardThemeKey ThemeName=Office2016White,
                                                   ResourceKey=HeaderPanelTemplate}"
                     TargetType="{x:Type ContentControl}">
        <ContentPresenter />
    </ControlTemplate>

</ResourceDictionary>
