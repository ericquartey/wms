<wms:WmsView x:Class="Ferretto.WMS.Modules.MasterData.LoadingUnitDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:businessModels="clr-namespace:Ferretto.Common.BusinessModels;assembly=Ferretto.Common.BusinessModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:modules="clr-namespace:Ferretto.Common.Utils.Modules;assembly=Ferretto.Common.Utils"
             xmlns:res="clr-namespace:Ferretto.Common.Resources;assembly=Ferretto.Common.Resources"
             xmlns:wms="clr-namespace:Ferretto.Common.Controls;assembly=Ferretto.Common.Controls"
             Title="{x:Static res:Navigation.LoadingUnitDetails}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             EnableHistoryView="True"
             Style="{DynamicResource SlideInRightToLeft}"
             mc:Ignorable="d">

    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto"
                              MaxWidth="400" />
        </Grid.ColumnDefinitions>

        <wms:ActionBar Margin="10,10,10,0"
                       HorizontalAlignment="Right"
                       Style="{DynamicResource ActionBarNotCustomizable}">
            <wms:ActionBarItem Command="{Binding EditCommand}"
                               Content="{x:Static res:General.Edit}"
                               CustomGlyphSize="24,24"
                               Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/editor/ic_mode_edit_black_24dp.png"
                               GlyphSize="Custom" />
            <wms:ActionBarItem Command="{Binding SaveCommand}"
                               Content="{x:Static res:General.Save}"
                               CustomGlyphSize="24,24"
                               Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/content/ic_save_black_24dp.png"
                               GlyphSize="Custom" />
            <wms:ActionBarItem Command="{Binding RevertCommand}"
                               Content="{x:Static res:General.Revert}"
                               CustomGlyphSize="24,24"
                               Glyph="pack://application:,,,/Ferretto.WMS.App.Themes;component/icons/material/content/ic_undo_black_24dp.png"
                               GlyphSize="Custom" />
        </wms:ActionBar>

        <ScrollViewer Grid.Row="1"
                      Margin="10,0,10,10"
                      DataContext="{Binding LoadingUnit}"
                      Focusable="False"
                      IsTabStop="True">
            <Grid Margin="0,0,5,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <GroupBox Grid.Row="0"
                          Grid.RowSpan="2"
                          Grid.Column="0"
                          Margin="0"
                          Header="{x:Static res:MasterData.LoadingUnitInformation}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <wms:TextBox Grid.ColumnSpan="3"
                                     Margin="0,0,20,0"
                                     Text="{Binding Code, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <wms:ComboBox Grid.Row="1"
                                      Grid.ColumnSpan="3"
                                      Margin="0,0,20,0"
                                      EditValue="{Binding LoadingUnitTypeId}"
                                      ItemsSource="{Binding Path=LoadingUnitTypeChoices}" />
                        <wms:ComboBox Grid.Row="2"
                                      Margin="0,0,20,0"
                                      EditValue="{Binding LoadingUnitStatusId}"
                                      ItemsSource="{Binding Path=LoadingUnitStatusChoices}" />
                        <wms:ComboBox Grid.Row="2"
                                      Grid.Column="1"
                                      Margin="0,0,20,0"
                                      EditValue="{Binding AbcClassId}"
                                      ItemsSource="{Binding Path=AbcClassChoices}" />
                        <wms:ComboBox Grid.Row="2"
                                      Grid.Column="2"
                                      Margin="0,0,20,0"
                                      EditValue="{Binding ReferenceType}"
                                      ItemsSource="{Binding Path=ReferenceTypeChoices}" />
                        <wms:TextBox Grid.Row="3"
                                     Grid.ColumnSpan="3"
                                     Height="100"
                                     Margin="0,0,20,0"
                                     IsMultiline="True"
                                     Text="{Binding Note, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="0"
                          Grid.Column="1"
                          Margin="0"
                          Header="{x:Static res:MasterData.CurrentCellInformation}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <wms:ComboBox Grid.ColumnSpan="2"
                                      Margin="0,0,20,0"
                                      EditValue="{Binding CellId}"
                                      ItemsSource="{Binding Path=CellChoices}" />
                        <wms:ComboBox Grid.Row="1"
                                      Margin="0,0,20,0"
                                      EditValue="{Binding CellPositionId}"
                                      ItemsSource="{Binding Path=CellPositionChoices}" />
                        <wms:ComboBox Grid.Row="1"
                                      Grid.Column="1"
                                      Margin="0,0,20,0"
                                      EditValue="{Binding CellPairing}"
                                      ItemsSource="{Binding Path=CellPairingChoices}" />

                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="1"
                          Grid.Column="1"
                          Margin="0"
                          Header="{x:Static res:MasterData.PhysicalInformation}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <wms:SpinEdit Margin="0,0,20,0"
                                      EditValue="{Binding Height, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <wms:SpinEdit Grid.Column="1"
                                      Margin="0,0,20,0"
                                      EditValue="{Binding Weight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <wms:SpinEdit Grid.Column="2"
                                      Margin="0,0,20,0"
                                      EditValue="{Binding HandlingParametersCorrection, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="2"
                          Grid.Column="0"
                          Grid.ColumnSpan="2"
                          Margin="0"
                          Header="{x:Static res:MasterData.Compartments}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <wms:WmsTrayControl Grid.Row="0"
                                            Margin="0"
                                            CanvasMinHeight="150"
                                            IsCompartmentSelectable="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.IsCompartmentSelectableTray, Mode=OneWay}"
                                            IsReadOnly="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.ReadOnlyTray, Mode=OneWay}"
                                            SelectedColorFilterFunc="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.TrayColoringFunc, Mode=OneWay}"
                                            SelectedItem="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.SelectedCompartment, Mode=TwoWay}"
                                            ShowBackground="True"
                                            Tray="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.Tray, Mode=OneWay}" />

                        <StackPanel Grid.Row="1"
                                    Margin="0,5,0,0">
                            <TextBlock Text="{x:Static res:MasterData.LoadingUnitWithNoCompartment}"
                                       Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.LoadingUnitHasCompartments, Converter={StaticResource BTVIC}}" />
                            <wms:WmsGridControl MinHeight="200"
                                                MaxWidth="2000"
                                                MaxHeight="200"                                                
                                                SelectedBusinessObject="{Binding SelectedCompartment, Mode=TwoWay}"                                                                                                
                                                ItemsSource="{Binding CompartmentsDataSource, Mode=OneWay}"
                                                Visibility="{Binding LoadingUnitHasCompartments, Converter={StaticResource BTVC}}">
                                <wms:WmsGridControl.View>
                                    <dxg:TableView>
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="RowDoubleClick">
                                                <wms:WmsHistoryViewAppear Id="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.SelectedCompartment.Id, Mode=OneWay}"
                                                                          StartModuleName="{wms:NameOf Type={x:Type modules:MasterData}}"
                                                                          StartViewName="{x:Static modules:MasterData.COMPARTMENTDETAILS}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </dxg:TableView>
                                </wms:WmsGridControl.View>

                                <wms:WmsGridControl.Columns>
                                    <dxg:GridColumn Width="200*"
                                                    FieldName="Code" />
                                    <dxg:GridColumn Width="200*"
                                                    FieldName="LoadingUnitCode" />
                                    <dxg:GridColumn Width="100*"
                                                    FieldName="Sub1" />
                                    <dxg:GridColumn Width="100*"
                                                    FieldName="Sub2" />
                                    <dxg:GridColumn Width="200*"
                                                    FieldName="Lot" />
                                    <dxg:GridColumn Width="200*"
                                                    FieldName="CompartmentStatusDescription" />
                                    <dxg:GridColumn Width="100*"
                                                    FieldName="Stock" />
                                    <dxg:GridColumn Width="100*"
                                                    FieldName="MaxCapacity" />
                                    <dxg:GridColumn Width="200*"
                                                    FieldName="ItemPairingDescription" />
                                </wms:WmsGridControl.Columns>

                                <wms:WmsGridControl.SortInfo>
                                    <dxg:GridSortInfo FieldName="Code"
                                                      SortOrder="Ascending" />
                                </wms:WmsGridControl.SortInfo>

                            </wms:WmsGridControl>

                        </StackPanel>

                    </Grid>
                </GroupBox>

            </Grid>
        </ScrollViewer>

        <ScrollViewer Grid.RowSpan="2"
                      Grid.Column="1"
                      Focusable="False"
                      IsTabStop="False">

            <Border DataContext="{Binding LoadingUnit}"
                    Style="{StaticResource InfoBoxBorderStyle}">

                <GroupBox Margin="20,0"
                          Header="{x:Static res:MasterData.OtherInformation}">
                    <StackPanel Orientation="Vertical">
                        <wms:InfoText HorizontalAlignment="Left"
                                      HorizontalContentAlignment="Left"
                                      Content="{Binding CreationDate}"
                                      Visibility="{Binding Path=CreationDate, Converter={StaticResource RTVC}}" />
                        <wms:InfoText HorizontalAlignment="Left"
                                      HorizontalContentAlignment="Left"
                                      Content="{Binding LastHandlingDate}"
                                      Visibility="{Binding Path=LastHandlingDate, Converter={StaticResource RTVC}}" />
                        <wms:InfoText HorizontalAlignment="Left"
                                      HorizontalContentAlignment="Left"
                                      Content="{Binding InventoryDate}"
                                      Visibility="{Binding Path=InventoryDate, Converter={StaticResource RTVC}}" />
                        <wms:InfoText HorizontalAlignment="Left"
                                      HorizontalContentAlignment="Left"
                                      Content="{Binding LastPickDate}"
                                      Visibility="{Binding Path=LastPickDate, Converter={StaticResource RTVC}}" />
                        <wms:InfoText HorizontalAlignment="Left"
                                      HorizontalContentAlignment="Left"
                                      Content="{Binding LastStoreDate}"
                                      Visibility="{Binding Path=LastStoreDate, Converter={StaticResource RTVC}}" />
                        <wms:InfoText HorizontalAlignment="Left"
                                      HorizontalContentAlignment="Left"
                                      Content="{Binding InCycleCount}"
                                      Visibility="{Binding Path=InCycleCount, Converter={StaticResource RTVC}}" />
                        <wms:InfoText HorizontalAlignment="Left"
                                      HorizontalContentAlignment="Left"
                                      Content="{Binding OutCycleCount}"
                                      Visibility="{Binding Path=OutCycleCount, Converter={StaticResource RTVC}}" />
                        <wms:InfoText HorizontalAlignment="Left"
                                      HorizontalContentAlignment="Left"
                                      Content="{Binding OtherCycleCount}"
                                      Visibility="{Binding Path=OtherCycleCount, Converter={StaticResource RTVC}}" />
                    </StackPanel>
                </GroupBox>

            </Border>

        </ScrollViewer>

    </Grid>
</wms:WmsView>
