<wms:WmsView x:Class="Ferretto.WMS.Modules.MasterData.LoadingUnitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:Ferretto.Common.Modules.BLL.Models;assembly=Ferretto.Common.Modules.BLL"
             xmlns:res="clr-namespace:Ferretto.Common.Resources;assembly=Ferretto.Common.Resources"
             xmlns:wms="clr-namespace:Ferretto.Common.Controls;assembly=Ferretto.Common.Controls"
             MinHeight="300"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Style="{DynamicResource SlideInLeftToRight}"
             mc:Ignorable="d">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <wms:Expander x:Name="FiltersExpander"
                      Grid.Column="0"
                      VerticalAlignment="Stretch"
                      VerticalContentAlignment="Stretch"
                      ExpandDirection="Left"
                      IsExpanded="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <wms:TileBar ItemsSource="{Binding Filters}"
                             SelectedItem="{Binding SelectedFilter, Mode=OneWayToSource}" />
            </Grid>
        </wms:Expander>

        <Grid Grid.Column="1">

            <wms:ActionBar HorizontalAlignment="Left"
                           Style="{DynamicResource ActionBarOverlayedWithGrid}">
                <wms:ActionBarItem Content="{x:Static res:General.Refresh}"
                                   CustomGlyphSize="24,24"
                                   Glyph="pack://application:,,,/Ferretto.WMS.Themes;component/icons/material/navigation/ic_refresh_black_24dp.png"
                                   GlyphSize="Custom" />
            </wms:ActionBar>

            <wms:WmsGridControl x:Name="MainGridControl"
                                MaxWidth="2000"
                                MaxHeight="5000"
                                Margin="3"
                                CurrentDataSource="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.SelectedDataSource, Mode=OneWay}"
                                ItemType="{x:Type models:LoadingUnit}"
                                SelectionMode="Row"
                                ShowBorder="False">
                <wms:WmsGridControl.View>
                    <dxg:TableView AllowEditing="False"
                                   AllowGrouping="False"
                                   AllowMoveColumnToDropArea="False"
                                   NavigationStyle="Row"
                                   SearchPanelHorizontalAlignment="Right"
                                   ShowGroupPanel="False"
                                   ShowIndicator="False"
                                   ShowSearchPanelMode="Always"
                                   ShowFilterPanelMode="ShowAlways">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="RowDoubleClick">
                                <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type wms:WmsView}}, Path=DataContext.ViewDetailsCommand}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </dxg:TableView>

                </wms:WmsGridControl.View>

                <wms:WmsGridControl.Columns>
                    <dxg:GridColumn Width="100*"
                                    FieldName="Code"
                                    Header="{x:Static res:BusinessObjects.LoadingUnitCode}" />
                    <dxg:GridColumn Width="250*"
                                    FieldName="LoadingUnitTypeDescription"
                                    Header="{x:Static res:BusinessObjects.LoadingUnitType}" />
                    <dxg:GridColumn Width="100*"
                                    FieldName="LoadingUnitStatusDescription"
                                    Header="{x:Static res:BusinessObjects.LoadingUnitStatus}" />
                    <dxg:GridColumn Width="80*"
                                    FieldName="AbcClassDescription"
                                    Header="{x:Static res:BusinessObjects.ItemClass}" />
                    <dxg:GridColumn Width="80*"
                                    FieldName="AreaName"
                                    Header="{x:Static res:BusinessObjects.Area}" />
                    <dxg:GridColumn Width="80*"
                                    FieldName="AisleName"
                                    Header="{x:Static res:BusinessObjects.Aisle}" />
                    <dxg:GridColumn Width="80*"
                                    FieldName="CellFloor"
                                    Header="{x:Static res:BusinessObjects.CellFloor_extended}" />
                    <dxg:GridColumn Width="80*"
                                    FieldName="CellColumn"
                                    Header="{x:Static res:BusinessObjects.CellColumn_extended}" />
                    <dxg:GridColumn Width="80*"
                                    FieldName="CellSide"
                                    Header="{x:Static res:BusinessObjects.CellSide_extended}" />
                    <dxg:GridColumn Width="80*"
                                    FieldName="CellNumber"
                                    Header="{x:Static res:BusinessObjects.CellNumber_extended}" />
                    <dxg:GridColumn Width="150*"
                                    FieldName="CellPositionDescription"
                                    Header="{x:Static res:BusinessObjects.CellPositionDescription}" />
                </wms:WmsGridControl.Columns>

            </wms:WmsGridControl>
        </Grid>

    </Grid>
</wms:WmsView>
