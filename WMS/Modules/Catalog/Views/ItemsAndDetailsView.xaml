<ctrls:WmsView x:Class="Ferretto.WMS.Modules.Catalog.ItemsAndDetailsView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:catalog="clr-namespace:Ferretto.WMS.Modules.Catalog"
               xmlns:ctrls="clr-namespace:Ferretto.Common.Controls;assembly=Ferretto.Common.Controls"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
               xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               Title="Anagrafica articoli"
               d:DesignHeight="450"
               d:DesignWidth="800"
               mc:Ignorable="d">
    <Grid>
        <Grid.Resources>

            <ctrls:BoolToVisibilityConverter x:Key="BTVC" />
            <ctrls:BoolToVisibilityConverter x:Key="BTVIC"
                                             Invert="True" />
        </Grid.Resources>

        <ContentControl Visibility="{Binding DetailsViewVisibility, Converter={StaticResource BTVIC}}">
            <catalog:ItemsView Grid.Column="1" />
        </ContentControl>
        <ContentControl Visibility="{Binding DetailsViewVisibility, Converter={StaticResource BTVC}}">
            <catalog:ItemDetailsView>

            <!--
            temporarily remove the details view, since we will be refactoring this
            <localctrls:ItemDetailsView />
            -->
            </catalog:ItemDetailsView>
        </ContentControl>
    </Grid>
</ctrls:WmsView>
