<wms:WmsView x:Class="Ferretto.WMS.Modules.Compartment.CompartmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:compartment="clr-namespace:Ferretto.WMS.Modules.Compartment"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wms="clr-namespace:Ferretto.Common.Controls;assembly=Ferretto.Common.Controls"
             Title="Compartment"
             d:DesignHeight="1080"
             d:DesignWidth="1920"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <!--<RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="2*" />-->
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <!--<compartment:InputCompartmentView Grid.Row="0" />-->
        <Grid Grid.Row="0"
              Width="500"
              Height="500">
            <Canvas Name="myCanvas"
                    Width="500"
                    Background="LightGray"
                    ToolTipService.ShowDuration="99999">
                <!--
                    MouseLeftButtonDown="myCanvas_MouseLeftButtonDown"
                    MouseMove="Window_MouseMove_1"
                -->
                <wms:WmsRulerControl x:Name="horizontalRuler"
                                     Width="1000"
                                     Height="20"
                                     Orientation="Horizontal"
                                     />
                <wms:WmsRulerControl x:Name="verticalRuler"
                                     Width="20"
                                     Height="900"
                                     Orientation="Vertical" />
            </Canvas>
        </Grid>
        <!--<wms:WmsTrayControl x:Name="Canvas"
                            Grid.Row="1"
                            Margin="0,0,0,0"
                            Compartments="{Binding LoadingUnit.Compartments, Mode=OneWay}"
                            LoadingUnit="{Binding LoadingUnit, Mode=OneWay}"
                            SelectedItem="{Binding CompartmentSelected, Mode=TwoWay}"
                            ShowBackground="False" />-->

        <!--<compartment:ListCompartmentsView Grid.Row="2" />-->

    </Grid>
</wms:WmsView>
