<wms:WmsView x:Class="Ferretto.WMS.Modules.Compartment.CompartmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:compartment="clr-namespace:Ferretto.WMS.Modules.Compartment"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wms="clr-namespace:Ferretto.Common.Controls;assembly=Ferretto.Common.Controls"
             Title="Compartment"
             d:DesignHeight="1080"
             d:DesignWidth="1920"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>
        <compartment:InputCompartmentView Grid.Row="0"
                                          CompartmentSelected="{Binding CompartmentInput, Mode=TwoWay}" />
        <wms:WmsTrayControl x:Name="Canvas"
                            Grid.Row="1"
                            Margin="0,0,0,0"
                            LoadingUnit="{Binding LoadingUnit, Mode=TwoWay}"
                            SelectedItem="{Binding CompartmentInput, Mode=TwoWay}"
                            ShowBackground="False" />
    </Grid>
</wms:WmsView>
