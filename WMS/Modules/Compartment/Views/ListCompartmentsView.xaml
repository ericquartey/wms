<UserControl x:Class="Ferretto.WMS.Modules.Compartment.ListCompartmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:Ferretto.Common.BusinessModels;assembly=Ferretto.Common.BusinessModels"
             xmlns:wms="clr-namespace:Ferretto.Common.Controls;assembly=Ferretto.Common.Controls"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <wms:WmsGridControl x:Name="MainGridControl"
                                MaxWidth="2000"
                                MaxHeight="5000"
                                Margin="3"
                                CurrentItem="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.CompartmentSelected, Mode=TwoWay}"
                                ItemType="{x:Type models:CompartmentDetails}"
                                ItemsSource="{Binding RelativeSource={RelativeSource AncestorType={x:Type wms:WmsView}}, Path=DataContext.Tray.Compartments, Mode=OneWay}"
                                SelectionMode="Row"
                                ShowBorder="False">

                <wms:WmsGridControl.View>
                    <dxg:TableView AllowEditing="False"
                                   AllowGrouping="False"
                                   AllowMoveColumnToDropArea="False"
                                   NavigationStyle="Row"
                                   SearchPanelHorizontalAlignment="Right"
                                   ShowFilterPanelMode="ShowAlways"
                                   ShowGroupPanel="False"
                                   ShowIndicator="False"
                                   ShowSearchPanelMode="Always" />

                </wms:WmsGridControl.View>

                <wms:WmsGridControl.Columns>
                    <dxg:GridColumn Width="100*"
                                    FieldName="Code" />
                    <dxg:GridColumn Width="400*"
                                    FieldName="Description" />
                    <dxg:GridColumn Width="100*"
                                    FieldName="ItemCategoryDescription" />
                    <dxg:GridColumn Width="100*"
                                    FieldName="AbcClassDescription" />
                    <dxg:GridColumn Width="50*"
                                    FieldName="TotalStock" />
                    <dxg:GridColumn Width="100*"
                                    FieldName="TotalReservedForPick" />
                    <dxg:GridColumn Width="100*"
                                    FieldName="TotalReservedToStore" />
                    <dxg:GridColumn Width="50*"
                                    FieldName="TotalAvailable" />
                </wms:WmsGridControl.Columns>
            </wms:WmsGridControl>
        </Grid>
    </Grid>
</UserControl>
