<UserControl x:Class="Ferretto.VW.InstallationApp.Views.SSVerticalAxisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:custom="clr-namespace:Ferretto.VW.CustomControls.Controls;assembly=Ferretto.VW.CustomControls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Ferretto.VW.InstallationApp.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:res="clr-namespace:Ferretto.Common.Resources;assembly=Ferretto.Common.Resources"
             mc:Ignorable="d">
    <DockPanel>
        <Label HorizontalAlignment="Center"
               Content="{x:Static res:InstallationApp.VerticalAxis}"
               DockPanel.Dock="Top"
               Style="{StaticResource StandardTitleTextStyle}" />
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Button Name="NextProcedureButton"
                    Grid.Column="1"
                    Height="60"
                    Content="{x:Static res:InstallationApp.NextButton}"
                    IsEnabled="{Binding}"
                    Style="{StaticResource StandardButtonStyle}" />
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel>
                <custom:CustomSensorControl BulletColor="{Binding VerticalZeroSensorFillColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            LabelText="{x:Static res:InstallationApp.ZeroVerticalSensor}" />
                <custom:CustomSensorControl BulletColor="{Binding EmergencyEndRunFillColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            LabelText="{x:Static res:InstallationApp.EmergencyEndRun}" />
                <custom:CustomSensorControl BulletColor="{Binding BrakeResistanceOverTemperatureFillColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            LabelText="{x:Static res:InstallationApp.BrakeResistanceOvertemperature}" />
                <custom:CustomSensorControl BulletColor="Blue"
                                            LabelText="ccc" />
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>
