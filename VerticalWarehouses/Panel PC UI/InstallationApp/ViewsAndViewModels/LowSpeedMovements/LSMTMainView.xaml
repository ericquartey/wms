<UserControl x:Class="Ferretto.VW.App.Installation.ViewsAndViewModels.LowSpeedMovements.LSMTMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/">
    <Grid Width="1024"
          Height="608">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ContentControl Name="LSMTNavigationRegion"
                        Grid.Column="0"
                        Content="{Binding LSMTNavigationRegionCurrentViewModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <ContentControl Name="LSMTContentRegion"
                        Grid.Column="1"
                        Content="{Binding LSMTContentRegionCurrentViewModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
