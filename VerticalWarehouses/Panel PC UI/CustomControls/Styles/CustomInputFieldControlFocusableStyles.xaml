<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:custom="clr-namespace:Ferretto.VW.CustomControls.Controls"
                    xmlns:local="clr-namespace:Ferretto.VW.CustomControls.Styles">

    <Style x:Key="VWAPP_CustomInputFieldControlFocusableStyle"
           TargetType="{x:Type custom:CustomInputFieldControlFocusable}">
        <Setter Property="BorderColor" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_BorderColor_Normal}" />
        <Style.Triggers>
            <Trigger Property="Highlighted" Value="false">
                <Setter Property="BorderColor" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_BorderColor_Normal}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="true">
                <Setter Property="BorderColor" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_BorderColor_Normal}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="BorderColor" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_BorderColor_TextBox_Disabled}" />
            </Trigger>
            <Trigger Property="Highlighted" Value="true">
                <Setter Property="BorderColor" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_BorderColor_Highlighted}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="VWAPP_CustomInputFieldControlFocusable_TextBlockStyle"
           TargetType="{x:Type TextBlock}">
        <Setter Property="Background" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_Background_Normal}" />
        <Setter Property="Foreground" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_TextBlock_Foreground_Normal}" />
        <Setter Property="FontFamily" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_TextBlock_FontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_TextBlock_FontSize}" />
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_TextBlock_Foreground_Disabled}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="VWAPP_CustomInputFieldControlFocusable_TextBoxStyle"
           TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_Background_Normal}" />
        <Setter Property="Foreground" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_TextBox_Foreground_Normal}" />
        <Setter Property="FontFamily" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_TextBox_FontFamily}" />
        <Setter Property="BorderThickness" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_TextBox_BorderThickness}" />
        <Setter Property="FontSize" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_TextBox_FontSize}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Focusable="false"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" Value="{DynamicResource VWAPP_CustomInputFieldControlFocusable_TextBox_Foreground_Disabled}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
