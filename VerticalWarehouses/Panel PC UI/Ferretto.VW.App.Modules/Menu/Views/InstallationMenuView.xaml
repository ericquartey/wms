<ctrls:View x:Class="Ferretto.VW.App.Menu.Views.InstallationMenuView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:controls="clr-namespace:Ferretto.VW.App.Controls.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:prism="http://prismlibrary.com/"
            xmlns:res="clr-namespace:Ferretto.VW.App.Resources;assembly=Ferretto.VW.App.Resources"
            xmlns:viewmodels="clr-namespace:Ferretto.VW.App.Menu.ViewModels"
            d:DataContext="{d:DesignInstance Type=viewmodels:InstallationMenuViewModel,
                                             IsDesignTimeCreatable=False}"
            d:DesignHeight="700"
            d:DesignWidth="1024"
            prism:ViewModelLocator.AutoWireViewModel="True"
            mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Margin="15,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <controls:PpcButton Grid.Row="0"
                                    Margin="15,15,0,0"
                                    Command="{Binding MenuElevatorCommand}"
                                    Content="{x:Static res:Menu.MenuElevator}" />

                <controls:PpcButton Grid.Row="1"
                                    Margin="15,15,0,0"
                                    Command="{Binding MenuBaysCommand}"
                                    Content="Baia" />

                <controls:PpcButton Grid.Row="2"
                                    Margin="15,15,0,0"
                                    Command="{Binding MenuCellsCommand}"
                                    Content="{x:Static res:Menu.MenuCells}" />

                <controls:PpcButton Grid.Row="3"
                                    Margin="15,15,0,0"
                                    Command="{Binding MenuLoadingUnitsCommand}"
                                    Content="{x:Static res:Menu.MenuLoadingUnits}" />

                <controls:PpcButton Grid.Row="4"
                                    Margin="15,15,0,0"
                                    Command="{Binding MenuAccessoriesCommand}"
                                    Content="{x:Static res:Menu.MenuAccessories}" />

                <controls:PpcButton Grid.Row="5"
                                    Margin="15,15,0,0"
                                    Command="{Binding ViewParametersCommand}"
                                    Content="Parametri" />

                <controls:PpcButton Grid.Row="6"
                                    Margin="15,15,0,0"
                                    Command="{Binding MenuOldCommand}"
                                    Content="Menu OLD" />
            </Grid>
        </Grid>

        <Grid Grid.Column="1"
              Margin="15,15,15,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.ColumnSpan="2"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Style="{StaticResource VWAPP_ViewTitleTextBlockStyle}"
                       Text="{x:Static res:Menu.InstallationStatus}" />

            <StackPanel Grid.Row="2"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">

                <controls:PpcButton Margin="15,15,0,0"
                                    Command="{Binding ViewStatusSensorsCommand}"
                                    Content="Stato sensori" />

                <controls:PpcButton Margin="15,15,0,0"
                                    Command="{Binding MenuMovementsCommand}"
                                    Content="{x:Static res:Menu.Movements}" />

            </StackPanel>
        </Grid>
    </Grid>
</ctrls:View>
