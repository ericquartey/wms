<UserControl x:Class="Ferretto.VW.App.Operator.Views.LoadingUnitOperationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Ferretto.VW.App.Controls.Controls;assembly=Ferretto.VW.App.Controls"
             xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls"
             xmlns:res="clr-namespace:Ferretto.VW.App.Resources;assembly=Ferretto.VW.App.Resources"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <UserControl.Resources>

        <Style x:Key="BorderTimeLineCirleTextBlock"
           TargetType="{x:Type TextBlock}">
            <Setter Property="Margin" Value="-1,-1,0,0" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="{StaticResource FontSize_High}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource FerrettoLightGray}" />
        </Style>

        <Style x:Key="OperationIndicatorStyle" BasedOn="{StaticResource BorderTimeLinePath}"
               TargetType="{x:Type Path}">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Stretch" Value="Fill" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Bottom" />
            <Setter Property="StrokeThickness" Value="2" />
            <Setter Property="StrokeThickness" Value="0" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-90"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AdjustIndicatorStyle" BasedOn="{StaticResource OperationIndicatorStyle}"  TargetType="{x:Type Path}">
            <Setter Property="Margin" Value="0 0 370 0" />
        </Style>

        <Style x:Key="PickIndicatorStyle" BasedOn="{StaticResource OperationIndicatorStyle}"  TargetType="{x:Type Path}">
            <Setter Property="Margin" Value="0 0 280 0" />
        </Style>

        <Style x:Key="PutIndicatorStyle" BasedOn="{StaticResource OperationIndicatorStyle}"  TargetType="{x:Type Path}">
            <Setter Property="Margin" Value="0 0 180 0" />
        </Style>

    </UserControl.Resources>

    <Grid Visibility="{Binding IsOperationVisible, Converter={StaticResource BTVC}}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource PpcBackground}"
                BorderBrush="{StaticResource PpcBorderBrush}"
                BorderThickness="{StaticResource PpcBorderThickness}"
                CornerRadius="2,0,0,2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.ColumnSpan="2"
                    Text="{Binding SelectedItemCompartment.ItemDescription}"/>

                <ctrls:PpcImage 
                   Grid.Row="1"
                   Grid.RowSpan="2"
                   Grid.Column="0"
                   Width="180"
                   Height="180"                   
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Path="{Binding SelectedItemCompartment.ItemId}"                   
                   Stretch="UniformToFill" />
                
                <ctrls:PpcSpinEdit Grid.Row="1"                               
                               Grid.Column="1"
                               Grid.ColumnSpan="2"
                               Margin="0,0,10,0"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Top"                                 
                               Increment="{Binding QuantityIncrement}"
                               Mask="{Binding QuantityTolerance, StringFormat=F{0}}"
                               EditValue="{Binding InputQuantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               IsEnabled="{Binding CanInputQuantity}"
                               LabelText="{Binding InputQuantityInfo}"/>

                <controls:PpcButton Grid.Row="2"
                                    Grid.Column="1"
                                    Margin="40,0,0,0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Top"
                                    Command="{Binding ConfirmOperationCommand}"
                                    Content="{x:Static res:OperatorApp.Confirm}"
                                    IsBusy="{Binding IsBusyConfirmingOperation}"
                                    />

            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Path  Style="{StaticResource AdjustIndicatorStyle}"
                   Visibility="{Binding IsAdjustVisible, Converter={StaticResource BTVC}}">
            </Path>

            <Path  Style="{StaticResource PickIndicatorStyle}"
                   Visibility="{Binding IsPickVisible, Converter={StaticResource BTVC}}">
            </Path>

            <Path  Style="{StaticResource PutIndicatorStyle}"
                   Visibility="{Binding IsPutVisible, Converter={StaticResource BTVC}}">
            </Path>
        </Grid>

    </Grid>
</UserControl>
