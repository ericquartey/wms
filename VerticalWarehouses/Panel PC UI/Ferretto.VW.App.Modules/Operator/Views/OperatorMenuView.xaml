<ctrls:View x:Class="Ferretto.VW.App.Operator.Views.OperatorMenuView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:controls="clr-namespace:Ferretto.VW.App.Controls.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:prism="http://prismlibrary.com/"
            xmlns:res="clr-namespace:Ferretto.VW.App.Resources;assembly=Ferretto.VW.App.Resources"
            d:DesignHeight="450"
            d:DesignWidth="800"
            prism:ViewModelLocator.AutoWireViewModel="True"
            mc:Ignorable="d">
    <Grid Margin="93,30,100,0">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <controls:PpcButton Grid.Row="0"
                                Width="{DynamicResource PpcButtonWidth}"
                                Height="{DynamicResource PpcButtonHeight}"
                                Margin="15,0,15,15"
                                Command="{Binding DrawerActivityButtonCommand}"
                                Content="{x:Static res:OperatorApp.NavigationMainMenuDrawerOperation}" />

            <controls:PpcButton Grid.Row="1"
                                Width="{DynamicResource PpcButtonWidth}"
                                Height="{DynamicResource PpcButtonHeight}"
                                Margin="15,0,15,15"
                                Command="{Binding ShowItemSearchCommand}"
                                Content="{x:Static res:OperatorApp.NavigationMainMenuItems}" />

            <controls:PpcButton Grid.Row="2"
                                Width="{DynamicResource PpcButtonWidth}"
                                Height="{DynamicResource PpcButtonHeight}"
                                Margin="15,0,15,15"
                                Command="{Binding ShowItemListsCommand}"
                                Content="{x:Static res:OperatorApp.NavigationMainMenuLists}" />

            <controls:PpcButton Grid.Row="4"
                                Width="{DynamicResource PpcButtonWidth}"
                                Height="{DynamicResource PpcButtonHeight}"
                                Margin="0,15,15,0"
                                Command="{Binding ShowOtherMenuCommand}"
                                Content="{x:Static res:OperatorApp.NavigationMainMenuOther}" />

        </Grid>

        <Grid Grid.Row="1"
              Grid.Column="1">
            <Image Grid.Row="1"
                   Grid.Column="2"
                   Margin="0"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Source="pack://application:,,,/Ferretto.VW.App.Controls;Component/Assets/background_machine.png"
                   Stretch="Uniform" />
        </Grid>

    </Grid>
</ctrls:View>
