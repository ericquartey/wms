<ctrls:View x:Class="Ferretto.VW.App.Modules.Operator.Views.ItemSearchMainView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:Resources="clr-namespace:Ferretto.VW.App.Resources;assembly=Ferretto.VW.App.Resources"
            xmlns:behaviors="clr-namespace:Ferretto.VW.App.Controls.Behaviors;assembly=Ferretto.VW.App.Controls"
            xmlns:controls="clr-namespace:Ferretto.VW.App.Controls.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:ei="clr-namespace:Microsoft.Expression.Interactivity.Core;assembly=Microsoft.Expression.Interactions"
            xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
            xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
            xmlns:in="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
            xmlns:keyboardBehaviors="clr-namespace:Ferretto.VW.App.Controls.Behaviors;assembly=Ferretto.VW.App.Controls"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:prism="http://prismlibrary.com/"
            xmlns:views="clr-namespace:Ferretto.VW.App.Modules.Operator.Views"
            d:DesignHeight="450"
            d:DesignWidth="800"
            prism:ViewModelLocator.AutoWireViewModel="True"
            mc:Ignorable="d">

    <ctrls:View.Resources>

        <Style x:Key="SearchTextBoxStyle"
               BasedOn="{StaticResource VWAPP_PpcTextBox_TextBoxStyle}"
               TargetType="{x:Type TextBox}">
            <Setter Property="Foreground" Value="{DynamicResource FerrettoDarkGray}" />
            <Setter Property="Background" Value="{DynamicResource FerrettoWhiteGray}" />
        </Style>

        <Style x:Key="IconButtonBorderStyle"
               TargetType="{x:Type Border}">
            <Setter Property="Padding" Value="15" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="{DynamicResource VWAPP_BACKGROUND}" />
        </Style>
        <Style x:Key="IconButtonBorderInvertStyle"
               BasedOn="{StaticResource IconButtonBorderStyle}"
               TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{DynamicResource FerrettoWhiteGray}" />
        </Style>

        <Style x:Key="IconButtonStyle"
               TargetType="{x:Type Button}">
            <Setter Property="BorderBrush" Value="{x:Null}" />
            <Setter Property="Background" Value="{x:Null}" />
            <Setter Property="FontSize" Value="{DynamicResource FontSize_Very_High}" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource FerrettoLightGray}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Style="{StaticResource IconButtonBorderStyle}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PpcButtonBackgroundPressed}" />
                                <Setter TargetName="border" Property="Cursor" Value="Hand" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PpcButtonBackgroundPressed}" />
                                <Setter TargetName="border" Property="Cursor" Value="Hand" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButtonInvertStyle"
               BasedOn="{StaticResource IconButtonStyle}"
               TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="{DynamicResource FerrettoMediumGray}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                Style="{StaticResource IconButtonBorderInvertStyle}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DropShadowEffect x:Key="MyEffect"
                          BlurRadius="20"
                          Opacity="1"
                          ShadowDepth="0"
                          Color="Blue" />
    </ctrls:View.Resources>

    <Grid Margin="15,0,0,0">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.ColumnSpan="2"
                    Orientation="Horizontal">
            <TextBlock Margin="3,10,15,10"
                       VerticalAlignment="Top"
                       Style="{StaticResource VWAPP_ViewTitleTextBlockStyle}"
                       Text="1.3" />
            <TextBlock Grid.ColumnSpan="2"
                       Margin="10,10,15,10"
                       VerticalAlignment="Top"
                       Style="{StaticResource VWAPP_ViewTitleTextBlockStyle}"
                       Text="{x:Static Resources:OperatorApp.ItemSearchHeader}" />
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Grid.ColumnSpan="2"
                   Height="Auto"
                   Margin="0"
                   VerticalAlignment="Top"
                   FontSize="{DynamicResource FontSize_Small}"
                   Style="{DynamicResource VWAPP_ViewTitleSmallTextBlockStyle}"
                   Text="{x:Static Resources:HelpDescriptions.HelpItemSearchDescription}"
                   TextWrapping="Wrap" />

        <Border Grid.Row="2"
                Margin="0,15,0,0"
                Background="{StaticResource PpcBackground}"
                BorderBrush="{StaticResource PpcBorderBrush}"
                BorderThickness="{StaticResource PpcBorderThickness}"
                CornerRadius="0">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"
                      Height="30"
                      Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Margin="0,0,10,0"
                               VerticalAlignment="Center"
                               FontSize="{DynamicResource FontSize_High}"
                               Foreground="{DynamicResource FerrettoWhiteGray}">
                        <Run Text="{x:Static Resources:OperatorApp.ItemSearchKeySearch}" />
                        <Run Text=":" />
                    </TextBlock>

                    <TextBox x:Name="SearchText"
                             Grid.Column="1"
                             Margin="0"
                             Style="{StaticResource SearchTextBoxStyle}"
                             Text="{Binding SearchItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Delay=250}">
                        <i:Interaction.Behaviors>
                            <behaviors:TextBoxAutoSelectBehavior />
                            <keyboardBehaviors:KeyboardPopupBehavior InactiveTimeout="00:01:00"
                                                                     KeyboardLabel="{x:Static Resources:OperatorApp.ItemSearchKeySearch}" />
                        </i:Interaction.Behaviors>
                    </TextBox>

                    <Button x:Name="ClearButton"
                            Grid.Column="2"
                            Width="70"
                            Margin="0,1,1,1"
                            BorderThickness="0"
                            Visibility="{Binding Path=SearchItem, Converter={StaticResource TTUI}}">
                        <Button.Style>
                            <Style BasedOn="{StaticResource IconButtonInvertStyle}"
                                   TargetType="{x:Type Button}">
                                <Setter Property="Foreground" Value="{DynamicResource FerrettoMediumGray}" />
                                <Setter Property="Background" Value="{StaticResource FerrettoPureWhite}" />
                                <Setter Property="BorderBrush" Value="{StaticResource FerrettoPureWhite}" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource FerrettoLightGray}" />
                                        <Setter Property="BorderBrush" Value="{StaticResource FerrettoPureWhite}" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource FerrettoLightGray}" />
                                        <Setter Property="Background" Value="{StaticResource FerrettoMediumGray}" />
                                        <Setter Property="BorderBrush" Value="{StaticResource FerrettoMediumGray}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <in:Interaction.Triggers>
                            <in:EventTrigger EventName="Click">
                                <ei:ChangePropertyAction PropertyName="Text"
                                                         TargetObject="{Binding ElementName=SearchText}"
                                                         TargetName="SearchText"
                                                         Value="" />
                            </in:EventTrigger>
                        </in:Interaction.Triggers>
                        <Button.Template>
                            <ControlTemplate>
                                <Grid Margin="0"
                                      Background="{TemplateBinding Background}">
                                    <icons:PackIconFontAwesome Width="{TemplateBinding Width}"
                                                               Height="{TemplateBinding Height}"
                                                               Margin="2"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"
                                                               Foreground="{TemplateBinding Foreground}"
                                                               Kind="BackspaceSolid" />
                                </Grid>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <views:ArticleDataGridView Grid.Row="1"
                                           Grid.Column="0"
                                           Margin="0,15,0,0"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Top" />

            </Grid>

        </Border>

        <Grid Grid.Row="2"
              Grid.Column="1"
              Margin="0,15,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Height="90"
                    MinHeight="90"
                    Margin="15,0"
                    Background="{DynamicResource PpcBackground}">
                <Border BorderBrush="{DynamicResource PpcBorderBrush}"
                        BorderThickness="{DynamicResource PpcBorderThickness}">
                    <Grid Grid.Column="8"
                          Grid.ColumnSpan="4">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Margin="8,4"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   FontSize="{StaticResource FontSize_Normal}"
                                   FontWeight="Normal"
                                   Foreground="{DynamicResource FerrettoLightGray}"
                                   Text="{x:Static Resources:OperatorApp.ItemMachinesAvailability}" />

                        <TextBlock Grid.Row="1"
                                   Margin="4,0,4,4"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="{StaticResource FontSize_Very_High}"
                                   Foreground="{DynamicResource PpcForeground}"
                                   Text="{Binding SelectedItem.MachinesInfo, TargetNullValue={x:Static Resources:General.TargetNullValue}, FallbackValue={x:Static Resources:General.TargetNullValue}}"
                                   TextWrapping="Wrap" />

                    </Grid>
                </Border>
            </Border>

            <Border Grid.Row="1"
                    Height="90"
                    MinHeight="90"
                    Margin="15,15,15,0"
                    Background="{DynamicResource PpcBackground}">
                <Border BorderBrush="{DynamicResource PpcBorderBrush}"
                        BorderThickness="{DynamicResource PpcBorderThickness}">

                    <Grid Grid.Column="8"
                          Grid.ColumnSpan="4">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Margin="8,4"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   FontSize="{StaticResource FontSize_Normal}"
                                   FontWeight="Normal"
                                   Foreground="{DynamicResource FerrettoLightGray}"
                                   Text="{Binding SelectedItem.MeasureUnit, StringFormat={x:Static Resources:OperatorApp.AvailableQuantity}}" />

                        <TextBlock Grid.Row="1"
                                   Margin="4,0,4,4"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="{StaticResource FontSize_Very_High}"
                                   Foreground="{DynamicResource PpcForeground}"
                                   Text="{Binding AvailableQuantity, TargetNullValue={x:Static Resources:General.TargetNullValue}, FallbackValue={x:Static Resources:General.TargetNullValue}}"
                                   TextWrapping="Wrap" />

                    </Grid>
                </Border>
            </Border>

            <ctrls:PpcSpinEdit Grid.Row="2"
                               Width="230"
                               Height="90"
                               Margin="15,15,15,0"
                               EditValue="{Binding InputQuantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               Increment="{Binding SelectedItem.PickIncrement}"
                               LabelText="{Binding SelectedItem.MeasureUnit, StringFormat={x:Static Resources:OperatorApp.RequestedQuantity}}"
                               Mask="{Binding SelectedItem.PickTolerance, StringFormat=F{0}}"
                               MinValue="0" />

            <controls:PpcButton Grid.Row="3"
                                Margin="15,15,15,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Command="{Binding RequestItemPickCommand}"
                                Content="{x:Static Resources:OperatorApp.ItemSearchCall}"
                                IsBusy="{Binding IsBusyRequestingItemPick}" />

            <controls:PpcButton Grid.Row="4"
                                Margin="15,15,15,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Command="{Binding ItemDetailButtonCommand}"
                                Content="{x:Static Resources:OperatorApp.CommonButtonDetail}" />

        </Grid>
    </Grid>
</ctrls:View>
