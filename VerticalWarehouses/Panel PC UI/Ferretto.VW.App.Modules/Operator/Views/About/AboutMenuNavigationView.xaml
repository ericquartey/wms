<ctrls:View x:Class="Ferretto.VW.App.Operator.Views.AboutMenuNavigationView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:controls="clr-namespace:Ferretto.VW.App.Controls.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:prism="http://prismlibrary.com/"
            xmlns:resources="clr-namespace:Ferretto.VW.App.Resources;assembly=Ferretto.VW.App.Resources"
            xmlns:viewmodels="clr-namespace:Ferretto.VW.App.Operator.ViewModels"
            d:DataContext="{d:DesignInstance Type=viewmodels:AboutMenuNavigationViewModel,
                                             IsDesignTimeCreatable=False}"
            d:DesignHeight="700"
            d:DesignWidth="1024"
            prism:ViewModelLocator.AutoWireViewModel="True"
            mc:Ignorable="d">

    <Grid Grid.Row="0"
          Margin="15,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:PpcButton Grid.Row="0"
                            Width="200"
                            Margin="0,0,15,15"
                            HorizontalAlignment="Left"
                            Abbrevation="3.1"
                            BorderBrush="{DynamicResource FerrettoGreen}"
                            Command="{Binding GeneralCommand}"
                            Content="Generali"
                            FontFamily="{DynamicResource VWAPP_ViewTitleTextBlockStyle}"
                            FontSize="{DynamicResource FontSize_High}"
                            Style="{DynamicResource PpcButtonMenuStyle}" />

        <controls:PpcButton Grid.Row="1"
                            Width="200"
                            Margin="0,0,15,15"
                            HorizontalAlignment="Left"
                            Abbrevation="3.2"
                            BorderBrush="{DynamicResource FerrettoGreen}"
                            Command="{Binding CountersCommand}"
                            Content="Contatori"
                            FontFamily="{DynamicResource VWAPP_ViewTitleTextBlockStyle}"
                            FontSize="{DynamicResource FontSize_High}"
                            Style="{DynamicResource PpcButtonMenuStyle}" />

        <controls:PpcButton Grid.Row="2"
                            Width="200"
                            Margin="0,0,15,15"
                            HorizontalAlignment="Left"
                            Abbrevation="3.3"
                            BorderBrush="{DynamicResource FerrettoGreen}"
                            Command="{Binding StatisticsCommand}"
                            Content="Statistiche"
                            FontFamily="{DynamicResource VWAPP_ViewTitleTextBlockStyle}"
                            FontSize="{DynamicResource FontSize_High}"
                            Style="{DynamicResource PpcButtonMenuStyle}" />

        <controls:PpcButton Grid.Row="3"
                            Width="200"
                            Margin="0,0,15,15"
                            HorizontalAlignment="Left"
                            Abbrevation="3.4"
                            BorderBrush="{DynamicResource FerrettoGreen}"
                            Command="{Binding AllarmCommand}"
                            Content="Allarmi"
                            FontFamily="{DynamicResource VWAPP_ViewTitleTextBlockStyle}"
                            FontSize="{DynamicResource FontSize_High}"
                            Style="{DynamicResource PpcButtonMenuStyle}" />

        <controls:PpcButton Grid.Row="4"
                            Width="200"
                            Margin="0,0,15,15"
                            HorizontalAlignment="Left"
                            Abbrevation="3.5"
                            BorderBrush="{DynamicResource FerrettoGreen}"
                            Command="{Binding DiagnosticsCommand}"
                            Content="Diagnostica"
                            FontFamily="{DynamicResource VWAPP_ViewTitleTextBlockStyle}"
                            FontSize="{DynamicResource FontSize_High}"
                            Style="{DynamicResource PpcButtonMenuStyle}" />

    </Grid>

</ctrls:View>
