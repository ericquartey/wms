<ctrls:View x:Class="Ferretto.VW.App.Installation.Views.VerticalOffsetCalibrationStep2View"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:controls="clr-namespace:Ferretto.VW.App.Controls.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:prism="http://prismlibrary.com/"
            xmlns:res="clr-namespace:Ferretto.VW.App.Resources;assembly=Ferretto.VW.App.Resources"
            xmlns:views="clr-namespace:Ferretto.VW.App.Installation.Views"
            prism:ViewModelLocator.AutoWireViewModel="True"
            mc:Ignorable="d">
    <Grid Margin="130,30,115,0">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="90" />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="3"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Style="{StaticResource VWAPP_ViewTitleTextBlockStyle}"
                   Text="{x:Static res:InstallationApp.VerticalOffsetCalibration}" />

        <views:VerticalOffsetCalibrationNavigationView Grid.Row="1"
                                                       Grid.ColumnSpan="3"
                                                       VerticalAlignment="Top" />

        <controls:PpcLabeledText Grid.Row="2"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Top"
                                 ContentText="{Binding Cell.Id}"
                                 LabelText="{x:Static res:InstallationApp.CurrentCell}" />

        <controls:PpcLabeledText Grid.Row="2"
                                 Grid.Column="1"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Top"
                                 MinWidth="150"
                                 ContentText="{Binding Cell.Position, StringFormat={}{0:F1}}"
                                 LabelText="{x:Static res:InstallationApp.CellHeight}" />

        <controls:PpcLabeledText Grid.Row="2"
                                 Grid.Column="2"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Top"
                                 ContentText="{Binding Cell.Side, Converter={StaticResource EWSC}}"
                                 LabelText="{x:Static res:InstallationApp.CellSide}" />
        
        <controls:PpcLabeledText Grid.Row="3"
                                 Grid.Column="0"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Top"
                                 ContentText="{Binding InitialPosition, StringFormat={}{0:F1}}"
                                 LabelText="{x:Static res:InstallationApp.InitialPosition}" />

        <controls:PpcLabeledText Grid.Row="3"
                                 Grid.Column="1"
                                 MinWidth="150"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Top"
                                 ContentText="{Binding CurrentPosition, StringFormat={}{0:F1}}"
                                 LabelText="{x:Static res:InstallationApp.CurrentPosition}" />

        <controls:PpcButton Grid.Row="4"
                            Grid.Column="2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Command="{Binding MoveUpCommand}"
                            Content="{x:Static res:InstallationApp.Up}"
                            IsBusy="{Binding IsElevatorMovingUp}" />

        <ctrls:PpcSpinEdit   Grid.Row="4"
                             Grid.RowSpan="2"                            
                             Grid.ColumnSpan="1"
                             Grid.Column="1"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"
                             EditValue="{Binding InputStepValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             LabelText="{x:Static res:InstallationApp.StepValue}" />

        <controls:PpcButton Grid.Row="5"
                            Grid.Column="2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Command="{Binding MoveDownCommand}"
                            Content="{x:Static res:InstallationApp.Down}"
                            IsBusy="{Binding IsElevatorMovingDown}" />

        <controls:PpcLabeledText Grid.Row="6"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Top"
                                 ContentText="{Binding CurrentVerticalOffset, StringFormat={}{0:F1}}"
                                 LabelText="{x:Static res:InstallationApp.CurrentOffset}" />

        <controls:PpcLabeledText Grid.Row="6"
                                 Grid.Column="1"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Top"
                                 MinWidth="150"
                                 ContentText="{Binding Displacement, StringFormat={}{0:F1}}"
                                 LabelText="{x:Static res:InstallationApp.Displacement}" />

        <controls:PpcButton Grid.Row="6"
                            Grid.Column="2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Command="{Binding ApplyCorrectionCommand}"
                            Content="{x:Static res:InstallationApp.Correct}" />



    </Grid>
</ctrls:View>
