<UserControl x:Class="Ferretto.VW.App.Installation.Views.VerticalResolutionCalibrationNavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls">
    <Grid>
        <ItemsControl ItemsSource="{Binding MenuItems}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type ctrls:NavigationMenuItem}">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Margin="0,0,15,0"

                                   Style="{DynamicResource VWAPP_CustomLabel_ContentTextBlockStyle}"
                                   Text="{Binding Description}" />
                        <Border Margin="0,0,15,0"
                                BorderBrush="{DynamicResource PpcButtonForeground}"
                                BorderThickness="0,0,0,4"
                                Visibility="{Binding IsActive, Converter={StaticResource BTVC}}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
