<ctrls:View
    x:Class="Ferretto.VW.App.Modules.Installation.Views.ParametersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:dxprg="http://schemas.devexpress.com/winfx/2008/xaml/propertygrid"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls"
    prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ctrls:View.Resources>
        <ctrls:PropertyGridCellTemplateSelector x:Key="PropertyGridCellTemplateSelector"/>

        <Style TargetType="dxprg:CategoryDefinition">
            <!--<Style TargetType="dxprg:CollectionDefinition">-->
            <Setter Property="ExpandButtonVisibility" Value="Collapsed" />
            <!--<Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <DockPanel Background="Orange">
                            <dx:SimpleButton Margin="2"
                                             Height="20"
                                             Width="20"
                                             ButtonKind="Toggle"
                                             Glyph="{dx:DXImage Image=Add_16x16.png}"
                                             IsChecked="{Binding IsExpanded, Mode=TwoWay}" />
                            <TextBlock VerticalAlignment="Center" FontWeight="Heavy" FontSize="20" Text="{Binding Header}" />
                        </DockPanel>
                    </DataTemplate>
                </Setter.Value>
            </Setter>-->
            <!--<Setter Property="RowTemplate">
                <Setter.Value>
                    <DataTemplate />
                </Setter.Value>
            </Setter>-->
        </Style>
    </ctrls:View.Resources>
    <Grid>
        <dxprg:PropertyGridControl SelectedObject="{Binding Parameters}" ExpandCategoriesWhenSelectedObjectChanged="True" AllowExpanding="Force">
            <!--<dxprg:PropertyGridControl.PropertyDefinitions>
                <dxprg:PropertyDefinition Path="Date" IsReadOnly="True">
                    <dxprg:PropertyDefinition.CellTemplate>
                        <DataTemplate>
                            <dxe:DateEdit Name="PART_Editor" IsEnabled="{Binding IsReadOnly, Converter={StaticResource booleanNegationConverter}}"/>
                        </DataTemplate>
                    </dxprg:PropertyDefinition.CellTemplate>
                </dxprg:PropertyDefinition>
                <dxprg:PropertyDefinition Path="Name"/>
                <dxprg:PropertyDefinition Path="Test" IsReadOnly="True">
                    <dxprg:PropertyDefinition.CellTemplate>
                        <DataTemplate>
                            <dxe:TextEdit Name="PART_Editor" IsEnabled="{Binding IsReadOnly, Converter={StaticResource booleanNegationConverter}}"/>
                        </DataTemplate>
                    </dxprg:PropertyDefinition.CellTemplate>
                </dxprg:PropertyDefinition>
            </dxprg:PropertyGridControl.PropertyDefinitions>-->
            <dxprg:PropertyDefinition Path="*"
                                      CellTemplateSelector="{StaticResource PropertyGridCellTemplateSelector}"
                                      InsertDefinitionsFrom="{dxprg:RootPropertyDefinition}"/>
        </dxprg:PropertyGridControl>
    </Grid>
</ctrls:View>
