<ctrls:View x:Class="Ferretto.VW.App.Installation.Views.ShutterEnduranceTestView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:controls="clr-namespace:Ferretto.VW.App.Controls.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:prism="http://prismlibrary.com/"
            xmlns:res="clr-namespace:Ferretto.VW.App.Resources;assembly=Ferretto.VW.App.Resources"
            prism:ViewModelLocator.AutoWireViewModel="True"
            mc:Ignorable="d">
    <Grid Margin="130,30,130,10">

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2"
                   VerticalAlignment="Top"
                   Style="{StaticResource VWAPP_ViewTitleTextBlockStyle}"
                   Text="{Binding BayNumber, StringFormat={x:Static res:InstallationApp.ShutterEnduranceTestTitle}}" />

        <controls:PpcLabeledText Grid.Row="1"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Top"
                                 ContentText="{Binding InputRequiredCycles}"
                                 LabelText="{x:Static res:InstallationApp.RequiredCycles}" />

        <controls:PpcTextBox Grid.Row="1"
                             Grid.Column="1"
                             HorizontalAlignment="Left"
                             VerticalAlignment="Top"
                             InputText="{Binding InputDelayBetweenCycles, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsExecutingProcedure, Converter={StaticResource BNC}}"
                             LabelText="{x:Static res:InstallationApp.DelayBetweenCycles}" />


        <StackPanel Grid.Row="2"
                    Margin="0,0,0,15"
                    HorizontalAlignment="Left">

            <controls:PpcSensorControl SensorState="{Binding Sensors.Open}"
                                       Text="{x:Static res:InstallationApp.ShutterOpen}" />
            <!--  optional sensor  -->
            <controls:PpcSensorControl SensorState="{Binding Sensors.MidWay}"
                                       Text="{x:Static res:InstallationApp.ShutterMidWay}" />

            <controls:PpcSensorControl SensorState="{Binding Sensors.Closed}"
                                       Text="{x:Static res:InstallationApp.ShutterClosed}" />

        </StackPanel>

        <controls:PpcLabeledText Grid.Row="3"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Top"
                                 ContentText="{Binding PerformedCyclesThisSession}"
                                 LabelText="{x:Static res:InstallationApp.PerformedCyclesThisSession}" />

        <controls:PpcLabeledText Grid.Row="3"
                                 Grid.Column="1"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Top"
                                 ContentText="{Binding CumulativePerformedCycles}"
                                 LabelText="{x:Static res:InstallationApp.CumulativePerformedCycles}" />

        <controls:PpcButton Grid.Row="4"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Command="{Binding StopCommand}"
                            Content="{x:Static res:InstallationApp.Stop}" />

        <controls:PpcButton Grid.Row="4"
                            Grid.Column="1"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Command="{Binding StartCommand}"
                            Content="{x:Static res:InstallationApp.Start}"
                            IsBusy="{Binding IsExecutingProcedure}" />

    </Grid>

</ctrls:View>
