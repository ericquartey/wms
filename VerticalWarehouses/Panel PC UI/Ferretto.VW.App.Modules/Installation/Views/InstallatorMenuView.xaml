<ctrls:View x:Class="Ferretto.VW.App.Installation.Views.InstallatorMenuView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:ctrls="clr-namespace:Ferretto.VW.App.Controls;assembly=Ferretto.VW.App.Controls"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:prism="http://prismlibrary.com/"
            d:DesignHeight="450"
            d:DesignWidth="800"
            prism:ViewModelLocator.AutoWireViewModel="True"
            mc:Ignorable="d">
    <!--
    <Grid>
    -->
    <!--
    <Image Grid.Column="1"
            Margin="0,80,100,30"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Opacity="0.3"
            RenderOptions.BitmapScalingMode="HighQuality"
            Source="pack://application:,,,/Ferretto.VW.App.Controls;Component/Assets/background_machine.png"
            Stretch="Uniform" />
    -->
    <Grid Margin="110,30,110,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ctrls:PpcMenuWrap Grid.ColumnSpan="2"
                           IsEnabled="{Binding AreItemsEnabled}"
                           ItemsSource="{Binding InstallatorItems}" />

        <ctrls:PpcMenuWrap Grid.Row="2"
                           Grid.ColumnSpan="2"
                           ItemsSource="{Binding SensorsItems}" />

        <ctrls:PpcMenuWrap Grid.Row="4"
                           Grid.ColumnSpan="2"
                           IsEnabled="{Binding AreItemsEnabled}"
                           ItemsSource="{Binding OtherItems}" />
    </Grid>
    <!--
    </Grid>
    -->
</ctrls:View>
