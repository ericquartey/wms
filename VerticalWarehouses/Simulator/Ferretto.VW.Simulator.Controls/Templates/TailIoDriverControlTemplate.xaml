<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrl="clr-namespace:Ferretto.VW.Simulator.Controls.Controls;assembly=Ferretto.VW.Simulator.Controls">

    <ControlTemplate x:Key="VWSIM_Grid_IoDriver">
        <Border Style="{StaticResource ResourceKey=VWSIM_Border_IoDriver}">
            <Grid Margin="-1"
                      Background="{StaticResource FerrettoMediumGray}">

                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"
                          Margin="5,5,5,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Grid.ColumnSpan="2"
                                       Style="{StaticResource VWAPP_CustomLabel_ContentTextBlockStyle}"
                                       Text="{Binding Id, StringFormat='IO DEVICE 0{0}'}"
                                       ToolTip="Id" />

                        <TextBlock Grid.Row="1"
                                       Grid.Column="0"
                                       Margin="0,0,5,0"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource VWAPP_CustomLabel_LabelTextBlockStyle}"
                                       Text="Output:" />

                        <ItemsControl Grid.Row="1"
                                          Grid.Column="1"
                                          Margin="0,5"
                                          ItemsSource="{Binding Outputs}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <ctrl:CustomSensorSquareControl Margin="0"
                                                                            HorizontalAlignment="Left"
                                                                            VerticalAlignment="Center"
                                                                            IsReadOnly="True"
                                                                            LabelText=""
                                                                            SensorState="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                            Style="{StaticResource VWSIM_CustomSensorSquareControlStyle}" />
                                        <TextBlock Margin="-2,2,0,0"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       Style="{StaticResource VWAPP_CustomLabel_LabelTextBlockStyle}"
                                                       Text="{Binding Description}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                    </Grid>

                    <custom:VWIcon Grid.Column="1"
                                       Height="36"
                                       HorizontalAlignment="Right"
                                       ColorizeBrush="{DynamicResource VWAPP_ActionButton_Foreground_Normal}"
                                       Source="pack://application:,,,/Ferretto.VW.Simulator;Component/Icons/device.png" />
                </Grid>

                <ItemsControl Grid.Row="1"
                                  Margin="0,5"
                                  ItemsSource="{Binding Inputs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ctrl:CustomSensorSmallControl Margin="5,5,9,5"
                                                               HorizontalAlignment="Left"
                                                               VerticalAlignment="Center"
                                                               LabelText="{Binding Description}"
                                                               SensorState="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                               Style="{StaticResource VWSIM_CustomSensorSmallControlStyle}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
    </ControlTemplate>

</ResourceDictionary>
