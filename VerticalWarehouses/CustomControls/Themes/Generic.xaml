<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Ferretto.VW.CustomControls.Controls">

    <Style TargetType="{x:Type local:CompartmentRectangle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CompartmentRectangle}">
                    <UserControl Name="CompartmentRectangleUserControl"
                                 Focusable="True">
                        <Border Name="border"
                                BorderBrush="Red"
                                BorderThickness="2">
                            <DockPanel Background="Red"
                                       Opacity=".2">
                                <TextBlock HorizontalAlignment="Center"
                                           VerticalAlignment="Top"
                                           DockPanel.Dock="Top"
                                           FontSize="15"
                                           Foreground="White"
                                           Text="{Binding ElementName=border, Path=ActualWidth}" />
                                <TextBlock HorizontalAlignment="Center"
                                           VerticalAlignment="Top"
                                           DockPanel.Dock="Left"
                                           FontSize="15"
                                           Foreground="White"
                                           Text="{Binding ElementName=border, Path=ActualHeight}" />
                            </DockPanel>
                        </Border>
                    </UserControl>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="Background" Value="Green" />
                            <Setter TargetName="border" Property="BorderBrush" Value="Green" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
