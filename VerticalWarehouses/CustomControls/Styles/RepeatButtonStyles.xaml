<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Ferretto.VW.CustomControls.Styles">
    <Style x:Key="IAPP_NavigationViewRepeatButtonStyle"
           TargetType="{x:Type RepeatButton}">
        <Setter Property="Height" Value="38" />
        <Setter Property="Background" Value="{StaticResource FerrettoMediumGray}" />
        <Setter Property="Foreground" Value="{StaticResource FerrettoPureWhite}" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontFamily" Value="{StaticResource HelveticaNeue-LightCond}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Name="border"
                            Background="{StaticResource FerrettoMediumGray}"
                            BorderBrush="Transparent"
                            BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Button.IsPressed" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="Transparent" />
                            <Setter TargetName="border" Property="Background" Value="{StaticResource FerrettoDarkGray}" />
                        </Trigger>
                        <Trigger Property="Button.IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{StaticResource FerrettoMediumGray}" />
                            <Setter TargetName="border" Property="Background" Value="{StaticResource FerrettoWhiteGray}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
