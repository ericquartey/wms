<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:custom="clr-namespace:Ferretto.VW.CustomControls.Controls"
                    xmlns:local="clr-namespace:Ferretto.VW.CustomControls.Styles">
    <Style x:Key="IAPP_ViewCustomInputFieldFocusableControlTextBoxStyle"
           TargetType="{x:Type TextBox}">
        <Setter Property="Foreground" Value="{DynamicResource IAPP_CCInput_Foreground_Normal}" />
        <Setter Property="FontFamily" Value="{DynamicResource IAPP_BodyFont}" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Focusable="false"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="border" Property="Opacity" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="IAPP_ViewCustomInputFieldControlFocusableStyle"
           TargetType="{x:Type custom:CustomInputFieldControlFocusable}">
        <Style.Triggers>
            <Trigger Property="Highlighted" Value="true">
                <Setter Property="BorderColor" Value="{DynamicResource IAPP_CCInput_Highlighted_Normal}" />
            </Trigger>
            <Trigger Property="Highlighted" Value="false">
                <Setter Property="BorderColor" Value="{DynamicResource IAPP_CCInput_NotHighlighted_Normal}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
