parameters:
  BuildConfiguration: 'debug'
  BuildPlatform: 'any cpu'

steps:

- template: ../../Pipelines/azure-pipeline-net4-nuget-template.yml
  parameters:
    RestoreSolution: 'VerticalWarehouses/Simulator.sln'


- template: ../../Pipelines/azure-pipeline-assembly-versioning-template.yml


- task: VSBuild@1
  displayName: 'Build solution Simulator.sln'
  inputs:
    solution: 'VerticalWarehouses/Simulator.sln'
    platform: ${{ parameters.BuildPlatform }}
    configuration: ${{ parameters.BuildConfiguration }}
    msbuildArchitecture: x64


- task: PublishSymbols@2
  displayName: 'Publish symbols path'
  inputs:
    SearchPattern: 'VerticalWarehouses/Simulator/**/bin/**/*.pdb'
    PublishSymbols: false
  continueOnError: true


- task: CopyFiles@2
  displayName: 'Copy Files to staging directory'
  inputs:
    SourceFolder: '$(system.defaultworkingdirectory)'
    Contents: 'VerticalWarehouses/Simulator/**/bin/${{ parameters.BuildConfiguration }}/**'
    TargetFolder: '$(build.artifactstagingdirectory)'


- task: PublishBuildArtifacts@1
  displayName: 'Publish Artifact: Panel PC App'
  inputs:
    PathtoPublish: '$(build.artifactstagingdirectory)/VerticalWarehouses/Simulator/Ferretto.VW.Simulator/bin/${{ parameters.BuildConfiguration }}/net471'
    ArtifactName: 'simulator_app'


